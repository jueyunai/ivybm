# 开发进度

## 2026-07-18

### Task 7 询盘表单、线索模型和反滥用

- 已新增 `LeadSources` 与共享 `Leads` Collection，并注册到 Payload Admin。`requestId` 和客户端 `idempotencyKey` 均由数据库唯一索引保护；公开端点的重复或并发重试只保留一条线索并返回首次 `requestId`，`website-contact` 来源由服务端幂等确保存在。
- 已落实三角色权限：公网不能直接读取或写入线索；admin 可完整管理；operator 可读写及分配；sales 只可读取、更新分配给自己的线索，不能改写来源、幂等键或负责人。`Leads.source` 保持必填，数据库外键使用 `ON DELETE RESTRICT`，避免删除来源后产生无来源线索。
- 已实现 `POST /api/inquiries` 的 JSON 与普通 HTML form 双入口，保存姓名、公司、国家、邮箱、可选电话、产品兴趣、需求、locale、来源 URL 和 UTM；不保存 IP、User-Agent 或其他非必要指纹。接口包含字段白名单校验、32KB 实际流式请求体上限、蜜罐、固定窗口限流（单进程每客户地址 10 分钟 5 次）、稳定错误码和通用 503 边界，不向前端暴露数据库异常。公网部署必须由受信 OpenResty 覆盖代理 IP header 并隔离应用端口，该项纳入 Task 14 验收。
- 已替换 Task 6 的演示联系表单，提供英文 / 阿语客户端校验、提交中、成功、限流与可恢复失败状态；客户端 UUID 降级路径仍生成合法 UUID v4。无 JavaScript 提交返回对应 locale 与 RTL 的最小结果页，成功和失败都会显示 request ID，失败同时显示稳定错误码。
- 已生成 `20260718_143701_task7_inquiry_leads` migration 和最新 Payload 类型；Task 7 migration 单独 down / up、全部七条 migration 逆序 reset / 重新 up 均已通过。数据库核验确认 Task 8 `KnowledgeChunks.document` 仍为 `ON DELETE CASCADE`，Task 7 `Leads.source` 为 `ON DELETE RESTRICT`，未修改任何已合并 migration。
- 当前累计验证通过：lint、typecheck、54 个单元测试、6 个 AI 网关契约测试、50 个 PostgreSQL 集成测试、22 个官网与询盘功能 E2E、36 个英文 / 阿语 desktop / tablet / mobile 视觉回归和 Next.js production build；重复提交、真实入库、UTM、并发幂等、权限、honeypot、限流、超大流式 body、阿语无 JavaScript 早期错误均有自动化覆盖。
- 本 Task 修改 `src/payload.config.ts`、新增 migration 和共享 `Leads` 契约，合并前必须请求 xuemusi Review，不适用负责人自检合并。Task 7 合并后，Task 9 才可开始对 `Leads` 的数据库集成。

### Task 13 分阶段依赖规则（docs-only，连接器尚未在仓库交付）

- 已将平台连接器拆为接口 / 纯逻辑、数据库集成和外部平台联调三个阶段。依赖未合并时，允许使用 TypeScript port/interface、fake repository、mock 和官方结构 fixture 并行完成 Webhook 验签、时间戳、事件幂等、payload 归一化与 Meta / WhatsApp 契约测试。
- Task 13 会话侧数据库 adapter 等待 Task 9 `Conversations` / `Messages`，发布侧 adapter 等待 Task 12 `PublishJobs` / `PublishLogs`；真实 Webhook 异步处理、发布执行、失败重试和人工补偿还等待 Task 10 `Jobs` 与 worker。对应 Collection、migration、Payload 注册和生成类型必须全部合并后，数据库阶段才可开始。
- 外部平台联调需要账号、授权或 staging；缺失时以官方 fixture 契约测试、配置说明和阻塞记录按 P1 / P2 验收。fixture / mock 通过只代表接口契约完成，不代表平台 `available`。
- 本 PR 只修订协作规则、实施计划和 PoC 记录，不交付 Task 13 连接器代码、Collection、migration、Payload 配置或生产 Webhook route；任何未 push 的本地实验均不计入仓库开发进度。
- 已新增 `docs/research/一期平台接入PoC记录.md`，记录 Messenger、Instagram、WhatsApp、Meta 发布、LinkedIn 和 TikTok 的目标状态口径、数据库依赖与外部资产清单。

### Task 8 知识库、文档切片、向量检索和模型网关

- 已承接 `feat/task-8-evaluation-baseline` 中从冻结 `aikefu` 原型提取的离线测评资产，并扩充到一期最低验收线：60 条合成案例（P0 42 / P1 12 / P2 6）和 10 个知识源登记全部保持 `mocked`，不含客户资料、真实报价、合同或凭据；结构校验、覆盖报告和 CSV 导出均不访问外部网络。
- 已将评测结构与 P0 安全门禁接入 `pnpm test:unit`，覆盖重复 ID、未知知识源、必备高风险场景、禁答声明、追问字段、转人工要求、覆盖缺口和 CSV 输出。当前种子集用于防止安全边界回退，不冒充完整上线效果评测；正式知识仍必须经过业务审核后才能从 mocked / draft 转为 reviewed。
- 已新增 `KnowledgeDocuments`、`KnowledgeChunks`、`PromptTemplates` 三个 Collection，并注册到统一 Payload Admin 的 Knowledge Base 分组。知识文档保存来源类型、来源文件 / URL、版本、英文 / 阿语 locale、正文、审核人和审核时间；admin / operator 可管理，sales 和匿名用户不可访问。
- 已建立知识审核与索引状态机：只有 `reviewStatus=reviewed` 且 `indexStatus=ready` 的文档可进入正式检索；来源正文、版本、语言或引用信息变化时会自动退回 draft、清除索引就绪状态。索引过程使用数据库原子状态抢占和本次运行时间戳校验，双并发请求只有一个可进入 processing，旧任务也不能覆盖新任务或来源变更后的状态。
- 已收紧审核链路的内部写入边界：operator 可维护知识文档和 prompt，但不能直接创建、改写或删除系统生成的 chunk，也不能伪造索引状态和审核时间；active / archived prompt 版本不可原地改写或退回 draft，变更必须新建版本。
- 已实现确定性英文 / 阿语文档切片，稳定 ID 同时绑定文档 ID、版本、语言、顺序和切片正文，并保留标题、版本、URL 等来源引用。索引服务分页读取全部历史切片，通过内部 AI 网关按条数和估算 token 上限分批生成向量，设置单文档字节数和切片数量上限，幂等更新切片、批量删除旧切片，并把向量写入 PostgreSQL 原生 `vector` 列。
- 已实现 pgvector 余弦相似度检索，支持 locale、数量和最低分过滤，按距离稳定排序并返回来源引用；查询正文通过 AI 网关生成向量，检索强制匹配实际 embedding 模型和维度，更换供应商期间的新旧向量可安全共存，不会因维度不同报错或跨模型错排；草稿、归档、索引处理中、索引失败及来源变化后的旧切片均不会进入结果。
- 已实现供应商无关的 `generateText` / `embed` AI 网关，统一默认模型、30 秒文本超时、15 秒向量超时、错误码、重试属性、request ID、token 用量和 USD 成本估算记录；usage sink 和其错误回调均与已成功的模型结果隔离，记录失败不会触发模型重试或重复计费。OpenAI-compatible adapter 按官方 Responses 与 Embeddings HTTP 结构实现，并通过注入 fetch / fake provider 完成测试，不访问真实网络或付费 API，不在错误中传播 token。
- 已生成 `20260718_053459_task8_knowledge_base` migration 和最新 Payload 类型。migration 创建知识库 / prompt 表、prompt 版本与单 active 约束，并增加由 migration 单独管理的无固定维度 pgvector 列，保留后续更换 embedding provider 的空间；`KnowledgeChunks.document` 外键使用 `ON DELETE CASCADE`，直接数据库删除文档也会清理切片；已完成 Task 8 migration 单独 down / up 和全部六条 migration 逆序 reset / 重新 up 验证。
- reviewer 回归覆盖已补齐：真实双并发索引、1001 条历史切片分页与 stable ID 复用、embedding 分批及文档 / 切片限制、直接 SQL 级联删除、usage sink 故障隔离；GitHub Quality gate 现显式运行 `pnpm test:contract`。
- 当前验证通过：lint、typecheck、37 个单元测试、6 个 AI 网关契约测试、41 个 PostgreSQL 集成测试、连续两次幂等 seed 和 Next.js production build。测试全程使用 fake provider、官方结构 fixture 和独立 PostgreSQL 18.4 + pgvector 0.8.5 测试库。
- 本 Task 修改 `src/payload.config.ts`、新增 migration，并形成 Task 9 / Task 12 将消费的知识检索与 AI 网关公共接口，PR 必须请求 jueyunai Review，不适用负责人自检合并。

### Task 6 英文 / 阿语官网、RTL 和 SEO

- 已按客户确认的 `references/website-prototype/` 重写正式 Next.js / Payload 官网，完成 `/en`、`/ar` locale 前缀路由及 Home、About、Products、Projects、News、Contact、产品详情、项目详情和文章详情；根路径跳转英文，未知公开内容返回 404。
- 已实现英文 LTR / 阿语 RTL 的共享导航、移动菜单、语言切换、Hero 轮播、产品筛选、产品规格表、内容卡片、详情页、页脚和联系表单校验状态；公开页面只通过正常 Payload access 读取已发布内容，不绕过 CMS 权限。
- 已补齐 canonical、双语 hreflang、动态 metadata、Open Graph、Organization / Article / Product JSON-LD、`sitemap.xml` 和 `robots.txt`；英文 / 阿语页面、公开 API 和稳定 locale URL 均有单元与 E2E 覆盖。
- 已扩展确定性英文 / 阿语 seed：3 个产品分类、3 个产品、6 个项目、3 篇文章，修复 Payload 数组项 localized 字段回写，数据库确认英文 / 阿语各 12 条产品规格；连续重复 seed 保持幂等，公开内容不含 Demo / Fake 文案或占位联系方式。
- 已新增 36 个不绑定操作系统后缀的视觉回归基线，覆盖 6 个关键页面 × 英文 / 阿语 × desktop / tablet / mobile；视觉测试隐藏 Next.js 开发工具浮层，避免开发态 badge 造成快照波动，并可在后续 Linux staging 复用同一组基线。浏览器手工检查确认移动菜单、语言切换、RTL、主交互和无横向溢出。
- 最终 diff 审查后补齐生产 SEO 和 CMS 媒体边界：production 缺失或配置非法 `NEXT_PUBLIC_SERVER_URL` 时 fail fast；sitemap 按 locale、已发布翻译和 `noIndex` 过滤；图片 / OG / logo / gallery 字段只允许选择图片，前端与 SEO 运行时也拒绝把 PDF 当图片；seed 会删除历史 `ivybm-demo-*` 公共媒体。
- 已清理阿语残留英文：产品意向选项、文章分类、404、导航 / 菜单 / 轮播 aria-label 均按 locale 输出；表单错误补充 `aria-invalid` 和 `aria-describedby`，有效提交只明确说明 Task 7 尚未启用真实询盘存储，不模拟发送成功。
- 已修复 readiness helper 导出导致 Next Route production build 失败的问题，将共享逻辑移至 `src/lib/readiness.ts`；标准 Turbopack production build 已通过。
- 同步 PR #11 / Task 8 后的最新累计门禁通过：lint、typecheck、46/46 单元测试、6/6 AI 网关契约测试、41/41 PostgreSQL 集成测试和 production build；Task 6 专项另已通过 18/18 功能 E2E、36/36 视觉 E2E、连续两次幂等 seed 及旧 Demo 媒体清理核验。
- 根目录 `design-qa.md` 当前为 `final result: blocked`：代码布局、排版、颜色、响应式和 RTL 已贴近客户原型，但正式建筑 / 工厂 / 产品 / 项目摄影素材尚未提供；现阶段仅使用仓库内自有开发占位图且无外部图片热链。正式素材到位后需替换图片、重新生成视觉基线并复跑 Design QA。
- 本 Task 不修改 migration、`src/payload.config.ts`、共享 Collection 或跨人公共契约，属于 jueyunai 官网 / SEO 自有板块；将以 Draft PR 提交，先审核代码与功能，视觉素材阻塞解除后再转 Ready。

### Task 5 CMS 权限、媒体处理和演示内容

- 已新增统一内容权限模块：`Pages`、`Products`、`Projects`、`Posts` 的匿名及 sales 普通读取仅返回 `_status=published`，`Downloads` 仅返回 `isActive=true`，`Media` 仅返回显式 `isPublic=true` 的资产，`ProductCategories` 和 `SiteSettings` 提供受控公开读取；内容创建、修改、删除及后台菜单仅允许 admin / operator，sales 不可修改 CMS 内容。
- 已保留 `internalNotes` 字段级访问保护，公开读取使用正常 Payload access，不使用 `overrideAccess: true`；集成测试会主动传 `draft: true` 尝试读取，确认草稿仍不会泄露，匿名响应也不包含内部备注。
- 已将 Media 收紧为 AVIF / JPEG / PNG / WebP / PDF；图片最大 8MB，PDF 最大 20MB，REST multipart 和 Local API 均执行大小校验。图片自动生成 400×300 thumbnail、768×576 card 和最长边 1600 的 large 版本，关闭 EXIF 等原始 metadata 保留；每个资产必须填写 alt 和版权 / 来源说明。
- 已新增内容、媒体与站点设置缓存失效 hook，覆盖英文 / 阿语首页、普通页面、产品、案例和 News 路径；发布、取消发布、改 slug、删除、公开媒体替换和 SiteSettings 更新都会触发对应 revalidate，产品分类变化还会失效所有产品详情动态路径。seed / 维护流程可显式关闭 revalidation，非 Next request 上下文不会阻断数据写入。
- 已新增确定性英文 / 阿语演示内容 seed，覆盖自有生成占位图片、PDF、Home / About / Contact、产品分类、产品、案例、文章、下载和 SiteSettings；不使用 Unsplash 或外部图片热链。seed 已连续执行两次，管理员、媒体、内容和多语言导航均保持幂等。
- 已生成 `20260718_015857_cms_media_policy` migration 和最新 Payload 类型；migration 为既有 Media 记录安全回填待核验版权来源，再设置 `source NOT NULL`，新增默认关闭的 `isPublic` 公开开关，并增加三组派生图片字段与索引。已在独立 PostgreSQL 18 测试库完成全部五条 migration 逆序 down、重新 up、新 migration 单独 down / up 和旧媒体数据回填验证。
- 当前验证通过：lint、typecheck、29/29 单元测试、32/32 PostgreSQL 集成测试、连续两次幂等 seed 和 Next.js production build。默认本地数据库卷检测到旧 PostgreSQL 目录布局，未删除或修改；本 Task 使用独立 Compose project / 测试卷完成验证。
- 本 Task 修改多个 CMS Collection、`src/payload.config.ts` 和 migration，PR 必须请求 xuemusi Review，不适用负责人自检合并。合并后下一主线任务为 Task 6：按客户确认原型高保真实现英文 / 阿语官网、RTL、SEO 和视觉回归。

## 2026-07-17

### 客户确认官网 UI / 交互验收基线

- 已确认 `references/website-prototype/` 是客户提供并确认的正式官网 UI / 交互验收基准，不是普通视觉参考；Task 6 使用 Next.js / Payload 重写时，必须高保真还原视觉层级、版式、色彩、字体、间距、组件状态、导航、Hero 轮播、产品筛选、规格表、表单状态、桌面 / 平板 / 移动响应式，以及英文 LTR / 阿拉伯语 RTL 效果。
- 技术实现允许替换为 locale-prefixed URL、SSR / SSG、Payload CMS 和服务端询盘，但只允许替换占位联系方式、外部演示图片、示例数据、模拟提交成功和纯客户端逻辑；任何主动视觉或交互改版必须先获得客户确认。
- 已同步修正需求、客户需求梳理、架构、迁移设计、实施计划、README 和原型说明，并把 Task 6 / Task 15 的截图视觉回归和高保真验收纳入计划；Task 15 staging 验收命令会显式运行 `website-visual.spec.ts`。

### Task 4 CMS 核心 Collections 和多语言字段

- 已新增 `Pages`、`ProductCategories`、`Products`、`Projects`、`Posts`、`Downloads` 六个官网内容 Collection 和 `SiteSettings` Global，并统一注册到 Payload Admin；后台菜单按 Website Content / Website Settings 分组，管理员可进入对应列表与创建页面。
- 已启用 Payload `en` / `ar` localization，默认英文、阿语 RTL、读取时允许 fallback；标题、简介、正文、产品参数、站点信息和 SEO 文案均按 locale 存储，中文只允许放在非公开、非 localized 的 `internalNotes` 后台备注中。
- 已新增共享 SEO Group（title、description、keywords、canonical、ogImage、noIndex）和稳定 slug 字段，六个官网内容 Collection 均具备多语言 SEO 数据结构；slug 不参与 localization，在单个 Collection 内使用数据库唯一索引，英文和阿语共用同一 URL slug。英文标题可自动生成仅含小写拉丁字母、数字和单连字符的 slug；阿语首建（包括草稿）必须显式填写稳定 slug，标题翻译更新不会自动改变既有 URL。
- `Pages`、`Products`、`Projects`、`Posts` 已启用 draft / publish 和最多 50 个版本；Products 关联分类、封面和多图，Projects / Posts / Downloads / SiteSettings 的 Media、Page 引用也已建立。既有 `Media.alt` 继续保持非 localized、必填和数据库兼容，媒体格式、尺寸和版权策略留给 Task 5。
- 已生成 `20260717_092625_cms_core` 与 `20260717_102058_cms_seo_completion` migration、Payload 类型和 Admin import map，未修改已合并的 initial / auth_audit migration。审计发现 Payload 自动生成的 cms_core down 顺序会先级联删除表再重复删除旧 relation 表上的外键，已把外键、索引和新增列的清理调整到删表之前。
- 已实测空库 fresh migrate、旧 schema 升级、四个 migration 全部 down、重新 up、旧 users / media / audit_logs 哨兵数据保留；CMS 集成测试覆盖 Media alt、en/ar 往返、唯一及合法 slug、阿语首建、draft→published、版本、关系展开、非法引用、六个 Collection 的 localized SEO、SiteSettings 和 locale/version 表，共 10 项通过。SiteSettings 测试会恢复运行前快照，不向共享测试库遗留单例数据。
- Payload Admin 冒烟通过：管理员登录 `/admin` 后可看到全部 CMS 菜单，Products 创建页正常显示 English locale、RichText、SEO、稳定 slug、Save Draft / Publish；生成 import map 后控制台无组件缺失错误。PR #7 合并后已通过普通 merge 同步最新 `origin/main`，并在独立 PostgreSQL 18 测试库重新验证四条 migration 完整 up、逆序 down、再 up，连续两次幂等 seed、CMS 专项 10/10、全量集成测试 23/23、Compose 数据持久化回归和 Next.js production build；lint、typecheck、13/13 单元测试同步通过。
- Task 5 必须显式为“已发布内容”和 `SiteSettings` 配置受控的匿名只读权限；公开前台读取不可直接使用 `overrideAccess: true`，否则 Payload 会同时绕过 `internalNotes` 字段级访问控制。`Media.alt` 本 Task 继续保持资产级、非 localized、必填设计，媒体策略在 Task 5 完成。
- 本 Task 修改 `src/payload.config.ts`、新增 migration，并提供后续官网 / AI 模块消费的 CMS 公共结构，PR 必须请求 xuemusi Review，不适用负责人自检合并。

### PostgreSQL 18.4 与 pgvector 0.8.5 基线适配

- 实际一期数据库环境已确认为 PostgreSQL 18.4 + pgvector 0.8.5，因此同步更新 Compose、GitHub Actions、架构和实施计划的版本基线，并将镜像固定到已验证的 `pgvector/pgvector:0.8.5-pg18` digest。
- PostgreSQL 18 的真实 `PGDATA` 位于 `/var/lib/postgresql/18/docker`，Compose 持久化卷已改为挂载父目录 `/var/lib/postgresql`；新增独立 Compose project 的自动验证，实测写入探针数据、执行 `docker compose down` 删除容器并重新创建后数据仍然存在，测试结束只清理自己的容器和卷。
- 测试库重置由 `migrate:fresh` 调整为按已应用 migration 严格逆序执行 `down` 后重新 `up`，避免应用角色重建 `public` schema 时删除由数据库管理员安装、且应用角色无权重新创建的 vector 扩展；每条回滚使用 Payload `createLocalReq` 和 request transaction 执行，保证未来 migration 内的 Local API 调用与当前事务一致；仍保留 `_test` / `_ci` 数据库名保护并新增 PostgreSQL URL 协议校验。
- 数据库集成测试新增 PostgreSQL / pgvector 精确版本断言和向量余弦距离排序，覆盖真实 RAG 基础能力；本修复不修改已合并 migration、`src/payload.config.ts` 或共享 Collection。
- 已通过 lint、typecheck、单元测试 9/9、数据库集成测试 13/13、Compose 数据持久化回归、两条 migration 逆序重置与重建、连续两次幂等 seed 和 production build。

### Task 3 用户认证、角色权限、审计和健康检查

- 已完成 `admin`、`operator`、`sales` 三角色权限模型：管理员可管理全部资源；运营可完整管理内容并读写会话 / 线索；销售仅能读写分配给自己的会话 / 线索。用户后台仅允许已认证角色进入，管理员才能创建 / 删除用户和修改角色，普通用户只能读取、更新自身账号。
- 已补齐 Payload 用户安全策略：密码至少 12 位、连续登录失败 5 次锁定 10 分钟、会话 / JWT 2 小时、生产 Cookie 使用 `Secure` 和 `SameSite=Lax`；production 缺失或使用不足 32 位的 `PAYLOAD_SECRET` 时直接拒绝启动。首个 Payload 用户自动成为管理员，后续未指定角色的用户默认为销售，seed 明确创建管理员角色。
- 已新增只允许管理员读取、普通 API 不可创建 / 修改 / 删除的 `audit-logs` Collection；用户创建、更新、删除和登录均在同一 Payload 请求 / 数据库事务内写入审计记录，只保存动作、资源、文档 ID 和操作者，不记录密码、token、连接串或异常详情。
- 已新增 `/api/health/live` 与 `/api/health/ready`：live 只检查进程并返回应用版本，ready 通过共享 Payload PostgreSQL pool 执行 `SELECT 1`；数据库异常返回固定 503 `{"status":"unavailable"}`，不回显内部错误或密钥。两个路由均强制动态执行并使用 Node runtime。
- 已生成 `20260717_083319_auth_audit` migration 和最新 Payload 类型，未修改已合并的 initial migration。migration 会为既有用户回填 `admin` 后再设置 `role NOT NULL`，避免升级现有环境时失去后台权限；已实测 fresh migrate、单独 down、旧 schema 插入遗留用户后重新 up，遗留用户角色正确为 `admin`。
- 已将 CI 的数据库专项测试步骤调整为运行全部 integration tests。当前本地验证通过：lint、typecheck、9 个单元测试、12 个 PostgreSQL 集成测试、连续两次幂等 seed、migration fresh / down / upgrade 和 Next.js production build。
- 本 Task 修改 `src/payload.config.ts`、新增 migration，并形成后续任务依赖的认证 / 审计基础，PR 必须请求 xuemusi Review，不适用负责人自检合并。

### Task 2 PostgreSQL、pgvector、迁移和种子数据

- 已完成 PostgreSQL 18.4 + pgvector 0.8.5 工程底座：Compose 基础配置默认不暴露端口，本地 override 仅绑定 `127.0.0.1`，数据库镜像固定为 `pgvector/pgvector:0.8.5-pg18@sha256:12a379b47ad65289572ea0756efc11b7c241a6662833e8af7038cd3b73d647e0`，并配置持久化卷、健康检查、扩展初始化和日志轮转。
- 已关闭 Payload 开发环境隐式 schema push，固定 `src/migrations` 为 migration 目录，使用 Payload 3.86 CLI 生成并验证初始 migration；新增 migrate、测试库可重建 reset、幂等演示管理员 seed 和数据库集成测试命令。
- 测试库 reset 仅允许数据库名以 `_test` 或 `_ci` 结尾，并按已应用 migration 严格逆序回滚和重建，保留由 DBA 管理的扩展；seed 要求显式提供演示管理员参数，不包含真实客户数据。针对 Payload 3.86 一次性脚本不会自动释放 PostgreSQL 连接的问题，reset 和 seed 在成功完成后显式结束一次性进程。
- GitHub Actions 已接入临时 pgvector service，并在 run [`29562289546`](https://github.com/jueyunai/ivybm/actions/runs/29562289546) 中通过依赖安装、lint、typecheck、单元测试、migration、fresh reset、连续两次 seed、数据库集成测试和 production build，完整 Quality gate 耗时 2 分 10 秒。
- 本机 Docker daemon 可用，但 Docker Hub / credential helper 当前超时，无法完成本地镜像拉取；真实数据库生命周期已由 GitHub runner 对同一 digest 镜像验证。PR #5 涉及 `src/payload.config.ts` 和初始 migration，必须等待 xuemusi review 后才能合并。

### PR 自检合并与强制 Review 边界

- PR #3 `ci: 添加 PR 基础质量门禁` 已在质量门禁通过、无冲突且不涉及共享结构的前提下，由负责人完成自检并合并到 `main`；`main` push 的 CI 同步通过。
- 已将协作规则调整为两条路径：项目初始化、CI、工程配置、文档及负责人自己板块内的独立改动允许在 CI 通过、PR 清单完成和完整 diff 自检后自行合并；共享结构、migration、跨人公共契约、双方板块边界及影响协作者在途任务的改动仍必须由另一名开发者 review。
- 已移除 CODEOWNERS 的全仓库默认双方 owner，仅为 `src/payload.config.ts`、migration 和五个共享 Collection 自动请求双方关注；跨人公共契约和板块边界由 PR 作者人工识别并请求 review，一期上线验收仍需两人共同确认。

### GitHub Actions 基础 CI

- 已在 Task 1 合并后创建独立 CI 变更，为 pull request 和 `main` push 配置自动质量门禁：依赖锁定安装、ESLint、TypeScript 类型检查、Vitest 单元测试和 Next.js production build。
- CI 使用单个 `ubuntu-latest` job、pnpm 缓存、15 分钟超时和同分支旧任务自动取消，不上传 artifact，减少 GitHub Free 私有仓库每月 2,000 分钟共享额度的消耗。
- CI 只使用无敏感信息的构建期占位环境变量；数据库集成测试、契约测试、生产镜像和部署流水线仍按 Task 2、Task 13、Task 14 分阶段接入。

### PR #1 仓库门禁规则合并

- PR #1 `chore: add private repository branch guardrails` 已完成 review 并合并到 `main`，正式启用 `AGENTS.md`、`CLAUDE.md`、CODEOWNERS、PR 模板和本地 `pre-push` hook 约束。
- Task 1 分支 `feat/task-1-app-foundation` 已变基到最新 `origin/main`，后续继续按短分支 + PR 流程推进，禁止直接 push `main`。

### Task 1 正式应用工程初始化

- 已完成 Payload CMS 3.86.0 + Next.js 16.2.6 + React 19.2.6 正式应用底座，使用 Node.js 24、pnpm 10.15.1 和 Payload PostgreSQL Adapter。
- 已提供 Next.js 前台、Payload Admin、REST、GraphQL、`Users`、`Media`、基础健康信息模块、环境变量示例、TypeScript、ESLint flat config、Prettier、Vitest 和 Playwright 配置。
- 官方模板下载受网络环境影响时，改为使用 Payload 官方仓库 `3.x/templates/blank` 并完成 PostgreSQL 转换；同时修复 Next.js 16 与旧 `FlatCompat` ESLint 配置不兼容问题，移除无业务价值的 `/my-route` 演示路由。
- 已通过 `pnpm install --frozen-lockfile`、`pnpm lint`、`pnpm typecheck`、`pnpm test:unit`（3/3）和 `pnpm build` 验证。
- 下一步：推送 Task 1 分支并创建 PR；review 合并后从最新 `main` 创建 Task 2 分支，推进 PostgreSQL / pgvector、Docker Compose 与环境配置。

## 2026-07-16

### 正式项目启动与迁移盘点

- 已确认 `/Users/zhiyun.lee/GitHub/builder/ivybm` 为正式项目目录，`/Users/zhiyun.lee/GitHub/demo/建材出海` 为前期资料与网站原型来源目录。
- 已完成迁移范围分析：需求基线、技术架构、必要平台调研、架构图源文件和网站原型应复制到正式项目；合同、报价、客户原始材料、生成预览、浏览器日志、临时文件及任何密钥不进入正式源码仓库。
- 已确认来源目录中的 `ivybm-website-code` 是静态展示原型，包含占位图片、占位联系方式和未接后端的询盘表单，只作为正式 Next.js / Payload 工程的 UI 与内容参考。
- 已发现正式目录本地 `.codex/config.toml` 含明文腾讯云访问密钥；该文件已被 `.gitignore` 排除，但相关密钥仍需立即在腾讯云侧轮换，后续改用环境变量或密钥管理服务。
- 下一步应先执行受控迁移并建立正式 Git 仓库，再搭建 Next.js + Payload + PostgreSQL + Docker Compose 工程底座和测试域名。

### 需求、技术方案与开发计划完整性审计

- 已审计一期需求说明、客户需求梳理、技术选型与部署架构、迁移设计及现有 23 个工作日阶段表。
- 需求文档已覆盖业务目标、模块范围、客户资料、验收口径、非一期边界和风险，可作为一期范围基线；但”首批 3 个会话平台 / 3 个发布平台”与平台官方接口可行性结论尚未完全一致，必须在编码前锁定平台和降级验收规则。
- 技术方案已锁定 Next.js + Payload CMS + PostgreSQL + pgvector + Docker Compose + 1Panel OpenResty 的模块化单体路线，并覆盖数据模型骨架、部署拓扑、资源边界和 ADR；仍需补充精确版本、环境划分、认证授权、密钥管理、备份恢复目标、可观测性、接口幂等与失败重试等实施级设计。
- 当前开发计划仅有 8 个阶段 / 23 个工作日的路线图，尚缺少可执行的任务拆分、文件路径、依赖顺序、测试矩阵、验收命令、里程碑和提交节点，因此不能视为完善。
- 决定在正式工程初始化前先完成文档闭环：统一平台口径，补齐非功能验收指标和实施级技术约束，再编写逐任务开发实施计划。

### 一期文档基线完善

- 已将 `docs/requirements/一期需求说明文档.md` 更新为 v1.1，取消固定“3 个会话平台 + 3 个发布平台”的必达承诺，明确平台范围暂定并按 P0 确定性交付、P1 条件性交付、P2 调研验证分级验收。
- 已锁定平台建议口径：官网聊天 P0 必达；WhatsApp Cloud API 与 Facebook Messenger / Instagram DM 作为 P1 条件接入或 PoC；TikTok 私信只做 P2 可行性验证；Facebook / Instagram 发布官方 API 优先；LinkedIn 无 API 权限时交付审核、格式化、素材下载和人工发布辅助流程。
- 已将 `docs/architecture/一期技术选型与部署架构规划.md` 更新为 v1.1，补充版本锁定、local / staging / production 环境、三角色权限、密钥管理、Webhook 验签与幂等、数据库任务队列、AI 网关、备份恢复、监控指标、CI/CD、回滚和测试策略。
- 已新增 `docs/plans/2026-07-16-一期开发实施计划.md`，将 23 个工作日拆为 15 个可验收任务，明确预计文件路径、依赖顺序、测试命令、第三方平台 mock / PoC 规则、完成定义和建议提交节点。
- 已完成需求、架构和开发计划交叉检查：未发现残留固定“3+3”承诺；平台交付等级、RPO 24 小时、RTO 4 小时、任务重试 5 次、备份保留和健康检查口径一致。
- 下一步为提交本轮文档基线；提交后从开发计划 Task 1 开始初始化正式 Payload / Next 工程。

### 需求与技术方案 v1.1 复核

- 对比 v1.0 → v1.1 的 Git 差异确认：上一轮审计指出的两个缺口已解决——平台承诺由”确定接入首批 3 个会话平台 / 3 个发布平台”改为 P0/P1/P2 条件分级交付；技术方案新增第 16 节实施级技术约束（版本管理、环境划分、认证审计、Webhook 幂等、AI 网关边界、备份恢复、可观测性、CI/CD、测试策略），并修正了 v1.0 中”2G 内存不足 / 升级到 2C4G”与文档自身”2C4G 单机”范围矛盾的表述。
- 交叉核对后新发现 3 处口径不一致，需在编写逐任务计划前拉齐：
  1. 高风险模块工期分配不一致：需求书按业务模块报价”海外社媒会话接入 5 天 + 飞书CRM 1.5 天”共 6.5 天、”发布接入 3 天”，但架构书对应工程阶段排期仅 3 天和 1 天，压缩超过一半，且恰好是双方都标注为第三方审核不可控的高风险模块。
  2. 架构文档 8.1 节会话接入表出现”TikTok / YouTube”，但需求文档全篇候选平台清单从未提及 YouTube，疑似模板残留，需确认后删除或补入需求范围。
  3. 需求书报价表中”方案梳理与竞品调研”（1 天、0 元）未在架构书 23 个工作日的 8 阶段排期中找到对应位置，需明确是否计入工程日。
- 技术方案层面另有 4 条非阻塞补强建议：内存风险应给出量化触发阈值而非”必要时升级”；AI 网关超时策略应区分实时会话与异步内容生成两种时延容忍度；知识库资料摄入 / 切片 / 多语言处理流程尚未设计；pgvector 规模预警应给量化监控指标。
- 结论：文档闭环基本达成，具备进入逐任务开发实施计划编写阶段的条件，建议先处理上述 3 项口径分歧。
- 进一步核对 `docs/plans/2026-07-16-一期开发实施计划.md` 发现问题1的具体落点：WhatsApp / Meta / LinkedIn / TikTok 连接器编码、Webhook 验签、契约测试和 PoC 文档全部集中在 Task 13（第 21 天，仅 1 天）；会话底层模型在 Task 9（第 11-14 天）随官网 AI 客服一并实现。而需求书报价按 ¥800/天计费，"海外社媒会话接入 5 天 + 发布接入 3 天"合计 ¥6400，对应的连接器编码实际只有 1 天，其余时间设计上是账号协调、等待第三方审核和 PoC / 阻塞证据整理。
- 已与客户确认：海外平台账号配合存在延迟是已知情况，客户接受整体计划按实际进展调整，不严格卡 23 个工作日完成会话接入与发布接入两个模块；因此排期与 Task 13 的时间分配不再调整，暂不需要额外催客户提前启动平台审核，也不需要在文档中补充"5 天/3 天报价构成"的说明。

### 免费私有仓库 main 分支约束

- 已确认仓库不能公开，当前 GitHub 套餐又无法为 private 仓库启用原生 branch protection；因此采用项目规则、PR 流程、CODEOWNERS 和本地 Git hook 的多层软约束方案。
- 已新增 `AGENTS.md` 作为 Codex 等编码代理的项目主规则，新增 `CLAUDE.md` 作为 Claude Code 入口，明确修改前创建 Task 短分支、禁止直接 push / force push `main`、共享结构必须 review、敏感资料不进入 Git。
- 已新增 `.githooks/pre-push` 和 `scripts/install-git-hooks.sh`；开发者首次 clone 后执行安装脚本，本机会阻止直接 push `main`。只有用户明确授权的紧急情况可用 `IVYBM_ALLOW_MAIN_PUSH=1` 绕过，并需事后补 PR 或记录。
- 已新增 `.github/pull_request_template.md` 和 `.github/CODEOWNERS`，统一 PR 验证清单，并为共享 Collection、Payload 配置和 migration 自动请求双方 review。CODEOWNERS 在当前套餐下是提醒机制，不等同于服务端强制审批。
- 已更新 `CONTRIBUTING.md` 和 `README.md`，说明免费私有仓库下的实际约束边界和 Git hook 安装方式。

### 双人协作分工与分支规范

- 已按用户圈定的范围确认板块分工："AI 客服与知识库"、"社媒会话与发布"两个板块交给协作开发者；"官网与 CMS"、"SEO / GEO 基础"、"飞书 CRM"、"内容工作台"四个板块，以及不属于 6 板块划分的"方案梳理与竞品调研"、"第一批部署上线、培训与试运营修复"两个收尾项，由 jueyunai 负责；上线验收需两人共同确认。已在需求文档第10节表格加"负责人"列，实施计划里程碑表同步加"负责人"列。
- 已评估用户最初提出的"一人一个长期分支、最后合并"方案：主要风险是 `payload.config.ts` 高频改动冲突、Payload/PostgreSQL migration 线性历史分叉、三个共享 Collection（`Leads`、`Conversations`/`Messages`、`GeneratedContents`/`PublishJobs`）字段变更缺乏协调，以及 staging 联调机制在开发期无法发挥作用。已改为推荐主干开发（trunk-based）+ 按 Task 粒度短分支高频合并，写入新建的 `CONTRIBUTING.md`，并明确 production 发布审批人为 jueyunai。
- 已标注关键跨人依赖并写入实施计划：Task 9（AI 客服，协作开发者）依赖 Task 7（询盘表单 / `Leads`，jueyunai）先合并到 `main`；Task 13（平台连接器，协作开发者）发布侧依赖 Task 12（内容工作台，jueyunai）产出的 `PublishJobs`。同时指出 Task 8（知识库）只依赖 Task 1-3，协作开发者理论上可与 jueyunai 的 Task 4-7 并行推进，两人总日历工期有机会短于顺序执行的 23 天，如需要可另行设计并行版排期。
- 协作开发者确认为 xuemusi，已在需求文档、实施计划和 `CONTRIBUTING.md` 中将占位符统一替换为实名；GitHub 账号待邀请协作者时补充。
- 已确认 GitHub 仓库 `jueyunai/ivybm` 已创建并完成首次推送（`docs: 完成一期文档基线` 与 `docs: 双人协作分工与分支规范闭环` 两次提交），当前为免费版私有仓库；xuemusi 的 write 邀请已发出，等待对方接受。
- 免费版私有仓库不支持 GitHub 原生 branch protection（API 返回需升级 Pro 或转 public）；已与用户确认暂不解决，`main` 分支门禁靠两人自觉遵守 `CONTRIBUTING.md` 约定，非技术强制，已同步更新 `CONTRIBUTING.md` 措辞避免文档与实际配置不符。
- Task 1（初始化正式 Payload / Next 工程）尚未进入代码实施；xuemusi 接受邀请后即可参与后续分支和 PR 协作。
- 进一步核对 `docs/plans/2026-07-16-一期开发实施计划.md` 发现问题1的具体落点：WhatsApp / Meta / LinkedIn / TikTok 连接器编码、Webhook 验签、契约测试和 PoC 文档全部集中在 Task 13（第 21 天，仅 1 天）；会话底层模型在 Task 9（第 11-14 天）随官网 AI 客服一并实现。而需求书报价按 ¥800/天计费，"海外社媒会话接入 5 天 + 发布接入 3 天"合计 ¥6400，对应的连接器编码实际只有 1 天，其余时间设计上是账号协调、等待第三方审核和 PoC / 阻塞证据整理。
- 已与客户确认：海外平台账号配合存在延迟是已知情况，客户接受整体计划按实际进展调整，不严格卡 23 个工作日完成会话接入与发布接入两个模块；因此排期与 Task 13 的时间分配不再调整，暂不需要额外催客户提前启动平台审核，也不需要在文档中补充"5 天/3 天报价构成"的说明。
