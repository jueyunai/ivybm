# 一期 MVP 关键闭环 E2E 与 Facebook 首轮测试用例

- 状态：自动化主线已合并且 `main` CI 成功，FB-PUB-04 浏览器负向补强已完成本地验证，待 current-head CI 与受控 canary（第 7 版，基于 `main@6066d5c`）
- 生效日期：2026-08-16
- 适用范围：MVP 本地 / CI 自动化、发布前受控 canary、客户验收证据
- 上位基线：[MVP 范围冻结与交付冲刺](./2026-08-10-MVP范围冻结与交付冲刺.md)

> 第 7 版状态：PR #101 已于 2026-08-25 合并，无邮箱社媒 Lead / FEISHU-02、WEB-CHAT-02、FB-PUB-01 及此前的 CMS-01、INQ-01、WEB-CHAT-01、Facebook Messenger 首轮闭环、readiness、FEISHU-01 和发布授权 revision 围栏均已进入 `main`；合并后 `main@6066d5c` 的 CI policy 与适用门禁成功。本次仅补强 FB-PUB-04 在默认关闭发布开关时的浏览器 UI、真实 HTTP 503 与数据库零副作用证据。真实 canary 尚未执行，fake / 本地结果不代表真实平台已联调。

## 1. 目的与范围

本文件把当前 MVP 的关键业务闭环转换为可执行测试用例，目标不是增加测试数量，而是证明用户可实际完成业务、外部副作用不会重复、系统不会把 fake 或未知结果伪报为成功。

当前首轮社媒验证顺序确认为：

- 消息入站：Facebook Messenger；
- 内容发布：Facebook Page 单图图文；
- Instagram、LinkedIn 不进入首轮自动化与真实 canary 的完成门槛；仍按上位冲刺基线记录为后续接入或 `conditional` / `blocked`，必须保留权限、配置和阻塞证据，不得显示为 `available`；
- 本文件不把 Facebook 首轮验证解释为三平台范围已经全部完成，也不以人工复制粘贴替代官方 API 发布。

## 2. 测试架构与真实性边界

### 2.1 推荐架构

本地 / CI 运行以下真实组件：

- Chromium 浏览器、Next.js、Payload、PostgreSQL；
- Portal / 官网真实路由与服务端 Auth / RBAC；
- 真实业务 Collection、数据库事务、Jobs 队列、领域状态机；
- 真实 Webhook HMAC 验签、幂等键、worker handler、authority / fence / CAS；
- 真实 Portal 读模型和最终用户可见状态。

只替换最后一道外部网络出口。但**注入点按进程分成两半，不是统一的一层 port**，这决定了各闭环的可实现顺序：

| 外部出口           | 调用发生在                                                | 现状                                                                                                                 | 可否在不改生产代码的前提下替换                                                                                                                                                                |
| ------------------ | --------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Meta 会话发送      | worker（`worker.ts` 注册 delivery handler）               | `fakeConversationOutboundPort.ts` 已在 `src` 内                                                                      | 可以：测试进程装配 worker 时注入                                                                                                                                                              |
| Meta 发布          | worker（publication handler）                             | `fakePublishingPort.ts` 已在 `src` 内                                                                                | 可以：同上                                                                                                                                                                                    |
| 飞书               | worker（lead sync / notify handler）                      | 已有 fake client 与契约测试                                                                                          | 可以：同上                                                                                                                                                                                    |
| AI（网站客服）     | **server 进程，请求内同步**                               | `runtime.ts:34` 创建真实 responder；responder 通过 `resolveAiGateway` 读取 CMS / 环境中的 OpenAI-compatible provider | **可以**：测试 fixture 将 provider `baseURL` 指向受控 stub；CI production 模式需使用受信任 HTTPS stub，或新增经 Review 的 loopback 豁免（当前不存在，见 §8 步骤 13）；不需要替换 `runtime.ts` |
| AI（平台会话回复） | worker（`worker.ts:197`，受 `conversationsEnabled` 门控） | 同 worker 侧                                                                                                         | 可以                                                                                                                                                                                          |

补充事实，避免实现时走错路：

- Meta Graph origin 是硬编码模块常量（`src/modules/platforms/meta/publishingOutbound.ts:31`、`conversationOutbound.ts:53`），**没有 base URL 注入口**；唯一 seam 是 `fetch` 参数（`publishingOutbound.ts:211`、`conversationOutbound.ts:152`）。因此"把 base URL 指向本地 stub 从而在网络层面保证到不了 Meta"这条路不存在。
- 服务端 fake AI 的可行实现是本地 OpenAI-compatible stub（`src/modules/ai/providers/` 目前只有 `openaiCompatible.ts`）。通过 AI Provider 的 `baseURL` 配置即可让 server 进程请求 stub；production 模式下必须满足 HTTPS 校验，不能使用真实付费 AI。

Playwright 不使用 `page.route()` 模拟 IVYBM 内部业务 API。复杂 provider 故障、并发、lease 和进程死亡注入优先放在 integration；浏览器 E2E 负责验证用户操作、真实数据库结果、队列结果和 Portal 展示。测试实现沿用仓库现有的 `getByRole` / `getByLabel` 等可访问语义定位。

### 2.2 发布前真实 canary

真实 canary 使用受控账号、实际 app / worker 和官方 API，验证本地 fake 无法证明的内容：

- Meta App、Page task、Webhook subscription 和 App Review；
- Page token、`pages_manage_posts` 和 Messenger 发送权限；
- 公开媒体能被 Meta 读取；
- 官方返回内容 ID / URL 与 Facebook 页面实际内容一致；
- 真实环境的 Worker wiring、网络和凭据读取。

**当前基础设施只有一台线上服务器，因此 canary 就是生产，不存在"等价真实环境"这个选项。** 由此以下几条不是可选项：

- 只使用受控测试账号与合成内容，禁止真实客户资料；
- 每条 canary 必须留脱敏关联 ID、时间与执行人；
- Facebook 发布 canary 会在真实 Page 留下内容，是否删除由测试负责人在 Facebook 页面人工确认并记录，禁止自动删除、禁止自动重跑；
- canary 执行前需 jueyunai 明确批准。

普通 CI 禁止调用真实 Meta、飞书或付费 AI，避免误发内容、消耗额度、外部波动和凭据泄漏。

### 2.3 明确不采用

- 不把现有 UI mock 用例视为真实业务闭环（`tests/e2e/chat-handoff.spec.ts` 与 `tests/e2e/admin-portal-conversations.spec.ts` 目前都用 `page.route()` mock 内部业务 API，保留其 UI 契约价值，但不作为闭环证据）；
- 不为测试引入通用外部平台模拟框架或修改冻结的 CI policy；
- 不在 `delivery_unknown`、真实账号未授权或 fake 结果下标记平台已发布 / 已发送；
- 不通过伪造邮箱绕过社媒身份到 Lead 联系渠道的产品缺口。

## 3. 测试数据与清理规则

- 测试实体名称沿用仓库既有 `e2e-` 前缀惯例（现有用例使用 `portal-e2e-space`、`portal-conversation-e2e`、`e2e-member-001`），Facebook 相关用例统一使用 `e2e-fb-` 前缀，便于前缀扫描清理；不引入新的后缀约定，不设无依据的长度上限，仅需满足对应字段的 `maxLength`；
- 邮箱使用 `example.invalid`，Page / sender / message ID 使用合成数字或明确的测试 ID；
- 不使用真实客户姓名、原始询盘、电话、token、合同、报价或媒体；
- 每个用例持有唯一 correlation ID，并贯穿页面响应、Conversation、Message、Lead、Handoff、Job、PublishJob、PublishLog 和 fake provider 请求；
- 用例在 `finally` 中按依赖顺序清理测试账号、映射、内容、媒体、会话、消息、Lead、Handoff、Jobs、command receipt 和审计记录；
- 失败分支也必须清理；清理失败应使测试失败，不得静默遗留共享测试数据。

## 4. P0 自动化用例

### 4.1 Portal CMS 到公开官网

| ID     | Setup                                                                       | Steps                                                                                                                                                 | Expected                                                                                                                                                            | Evidence                         |
| ------ | --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- |
| CMS-01 | admin；EN / AR 测试内容；产品、案例、文章各一条；可恢复的 SiteSettings 快照 | 在 `/dashboard` 发布三类内容并验证官网；编辑后刷新；下架并再次访问；在 `/dashboard/settings` 修改站点名称、说明和联系资料后验证官网页头、页脚与联系页 | 三类内容各自的 slug 在编辑前后保持稳定；站点资料 EN / AR 同步；英文 / 阿语与 RTL 正确；修改后公开页更新；下架后 404；全程不进入 `/admin`；测试结束恢复 SiteSettings | Playwright + Payload 读回 + 截图 |

> 已完成：`tests/e2e/admin-portal-content.spec.ts` 已覆盖产品、案例、文章的 EN / AR 发布、公开读取、RTL、同 slug 编辑和下架 404；SiteSettings 的 EN / AR 站点资料同步也已进入同一 CMS suite。当前 `main@64e2627` 的 website 基线为 19/19、skipped=0。

### 4.2 官网询盘到 Lead / 飞书

| ID     | Setup                                        | Steps                                                                                    | Expected                                                                                                                 | Evidence                                       |
| ------ | -------------------------------------------- | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------- |
| INQ-01 | active fake Feishu mapping；EN / AR 合成询盘 | 浏览器提交表单；读取响应关联 ID；执行 Lead sync / notification Job；使用相同幂等输入重放 | 页面、Lead、Portal、fake Feishu 使用同一 `requestId` / `leadId`；仅一个 Lead、一个远端客户记录；每种通知 intent 最多一次 | Playwright + PostgreSQL + fake Feishu 调用记录 |

> 已完成：INQ-01 已进入 CI 会排程的 `tests/e2e/website.spec.ts`，覆盖浏览器提交、Lead、Portal、fake Feishu 和重放幂等；`tests/e2e/inquiry.spec.ts` 继续保留 targeted 补充验证。当前 `main@64e2627` 的 website 基线为 19/19、skipped=0。

### 4.3 官网 AI 客服到人工跟进

| ID          | Setup                                                                                               | Steps                                                                                                                      | Expected                                                                                                  | Evidence                                   |
| ----------- | --------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| WEB-CHAT-01 | deterministic EN / AR responder（provider `baseURL` 指向受控 stub）；active mapping；admin/operator | 完成 2–3 轮初筛；提供公司、国家、产品、项目阶段、面积、图纸、预算 / 采购计划、时间和工作邮箱；进入 Portal 接管、回复、解决 | 资格状态逐轮持久化；恰好一个 A Lead 和一个 Handoff；飞书 upsert / 提醒幂等；`human_active` 后 AI 不再回复 | Playwright + PostgreSQL + fake AI / Feishu |
| WEB-CHAT-02 | 高风险语料；AI / 知识不可用故障注入                                                                 | 发送价格、交期、认证等承诺问题；分别禁用 AI 或知识；重放相同命令                                                           | 高风险不调用 AI；不可用时提供可恢复的人工路径；不伪造回答；重复命令不新增 Lead、Handoff 或通知            | Playwright + integration                   |

> WEB-CHAT-01 / 02 已由 `tests/e2e/website-chat-real.spec.ts` + `website-chat.support.ts` 完成真实数据库、受控 AI stub、Lead、Handoff、Portal 与 fake Feishu 闭环；`tests/e2e/chat-handoff.spec.ts` 继续保留 UI 契约价值。WEB-CHAT-02 新增无已审核知识、provider 503 与价格 / 交付 / 认证高风险三条 fail-closed 路径，均只创建一个可恢复 Handoff，不生成 AI 伪答复、Lead 或飞书通知；相同命令重放不重复，高风险路径在 provider 调用前短路。PR #100 后标题断言已同步为“官网访客 #短 ID”，本地 chat suite 5/5、skipped=0。

### 4.4 Facebook readiness 与其他平台诚实状态

| ID          | Setup                                                                                                                                         | Steps                                                                                            | Expected                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Evidence                                                  |
| ----------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------- |
| FB-READY-01 | **本地 readiness 环境，`ADMIN_PORTAL_PUBLISHING_ENABLED=true`**；Facebook Page 测试账号；Meta App 配置 fixture；可选 controlled-test evidence | 读取 `/dashboard/platforms` 与 readiness 接口；另执行一次 Worker heartbeat / 一次受控 Jobs smoke | 无 controlled-test evidence 时，Page connection 与两项 capability 为 `ready-for-controlled-test`；有已通过 evidence 时允许为 `available`；Messenger inbound 与 publishing 的平台审核状态为 `approved`；授权 scopes 包含 `pages_manage_posts`；Webhook allowlist、签名配置和发布运行时无 missing；Portal readiness 不伪报 Worker 健康，Worker 健康由独立 process / Jobs 证据证明。CI 默认开关为 `false` 时，必须预期 `publishing_disabled`，不得把该结果当作完整 readiness 通过 | Playwright + readiness 读回 + Worker process / Jobs smoke |
| READY-02    | 未连接或明确 blocked approval 的 Instagram / LinkedIn 账号 fixture                                                                            | 读取 `/dashboard/platforms` 与 readiness 接口                                                    | Portal 展示实际 `action-required` 或 `blocked`；项目验收记录可映射为 `conditional` / `blocked`；不出现 `available`，不提供可用发布入口；阻塞原因可读且不泄露配置                                                                                                                                                                                                                                                                                                               | Playwright + readiness 读回                               |

> READY-02 为本次新增：§1 与 §7 都要求"不得显示为 available"，第 1 版却没有验证用例。`tests/e2e/admin-portal-platforms-operations.spec.ts:138`（TikTok 不出现在客户流程）是同类断言的现成先例。

### 4.5 Facebook Messenger 入站、AI 与人工接管

| ID        | Setup                                                                               | Steps                                                                                                   | Expected                                                                                                                                                                                        | Evidence                                       |
| --------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| FB-IN-01  | 数字 Page / sender ID；合法 app secret；deterministic responder；fake Meta outbound | 向真实 `/api/webhooks/meta` POST 原始 JSON 与 HMAC；执行 event Job 和 delivery Job                      | HTTP 接收后仅一个 durable Job；创建一个 Conversation 和访客 Message；AI 产生一个 pending Message / delivery intent；fake Meta 调用一次；最终 Message=`sent`、intent=`accepted`、Job=`succeeded` | HTTP 响应 + PostgreSQL + fake Meta request     |
| FB-IN-02A | FB-IN-01 会话；active Feishu mapping                                                | 第二条 signed event 提供完整 A 级信号和测试邮箱；执行 event / Feishu Jobs；在 Portal 读回               | 复用同一 Conversation；恰好一个 `chat-lead:<sessionId>` Lead、一个 Handoff；Lead 与会话关联；一个飞书 upsert；`new_lead`、`high_intent`、handoff 各最多一次通知；Portal 会话与 Lead 视图一致    | PostgreSQL + fake Feishu payload + Portal 读回 |
| FB-IN-02B | 已验签 Page / sender / thread；不提供邮箱或电话                                     | 第二条 signed event 提供除邮箱外的完整 A 级信号                                                         | 平台身份作为可持续联系渠道；仍创建可跟进 Lead，保存 `facebook-messenger`、Page、sender、thread 来源；不得伪造邮箱                                                                                | PostgreSQL + fake Feishu + Portal E2E           |
| FB-IN-03  | FB-IN-02 会话；真实 Portal admin/operator                                           | 进入 `/dashboard/conversations`；接管；发送人工回复；执行 delivery Job；解决会话；重放 operator command | `handoff_requested -> human_active -> resolved` 合法；人工回复创建 `human_active` intent；fake Meta 校验 Page、recipient、正文并只调用一次；重放不重复发送                                      | Playwright + PostgreSQL + fake Meta            |
| FB-IN-04  | 错误签名、错误 recipient、非 allowlist、blocked / disabled 账号 fixture             | 分别 POST；比较请求前后数据库计数                                                                       | 返回受控 4xx / 503；Jobs、Conversation、Message、Lead、Handoff、飞书 Job 和外部调用均无新增；响应不泄露配置                                                                                     | HTTP + 零副作用快照                            |
| FB-IN-05  | 第一条 AI delivery 尚在 queued；第二条消息触发高意向 Handoff                        | 先提交两条入站，再尝试执行旧 AI delivery Job                                                            | Handoff 成为权威状态；旧 AI Job 在 provider I/O 前被阻断；fake Meta 零调用；新入站仍被持久化                                                                                                    | Integration fence / authority 证据             |
| FB-IN-06  | FB-IN-01 / 02 的已完成 event 与 command                                             | 重放相同 event；改变 transport receipt key 重试；模拟 worker 在业务 commit 后、Job ACK 前退出           | 只保留一个外部 Message、Lead、Handoff 和每种通知 / 发送；lease reclaim 后安全完成                                                                                                               | Integration + 数据库计数                       |

FB-IN-04 / 05 / 06 的原子能力在现有集成测试中已有覆盖（见 §8），本文件只补跨模块闭环与 Facebook 专属断言。

> FB-IN-01、FB-IN-02A、FB-IN-02B、FB-IN-03、FB-IN-04/05/06 与 readiness 首轮自动化均已合并；FB-IN-02B 已覆盖 signed webhook → 无邮箱 / 电话 A Lead → fake Feishu → Portal 读回 → 重放不重复的浏览器闭环，共享 Lead 结构与 migration 已由 `xuemusi` Review。真实 Messenger canary 尚未执行。

### 4.6 AI 内容审核到 Facebook Page 图文发布

发布开关 `ADMIN_PORTAL_PUBLISHING_ENABLED` 由 `process.env` 读取，且**同时拦截 UI 与真实 HTTP 命令层**：`src/app/api/portal/content-studio/[id]/route.ts:117` 调用 `publishContentStudioNow` 时不注入 `environment`，落回 `src/admin-portal/modules/content-studio/publishContentStudio.ts:352` 的默认值，在 `:367` 抛 `content-studio-publishing-disabled` 503。CI 固定为 `false`（`.github/workflows/ci.yml:235`）。集成测试通过 `environment` 参数注入 `'true'` 只能证明进程内直调命令层，**不能证明 UI → HTTP → 命令闭环**。

因此本轮采用的取舍是：**保留"CI 不具备发布能力"这一安全不变量，接受发布 happy path 不进 CI 门禁**。这是安全与覆盖的取舍，不是已经证明了发布闭环。

| ID         | Setup                                                                                                                                                          | Steps                                                                                                                                                | Expected                                                                                                                                                                          | Evidence                                                                                                                      |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| FB-PUB-01  | **本地显式设置 `ADMIN_PORTAL_PUBLISHING_ENABLED=true`**；approved Facebook Page；受控 JPEG / PNG；fake Meta publishing                                             | 工作台创建文案 / 采用资产；提交审核并批准；仅选择 Facebook；点击一次立即发布；执行初始 mutation、durable status obligation 与 continuation；刷新 Portal | 一个 PublishJob、三个 revision-scoped Jobs（`execute:r0`、`status:r1`、`execute:r1`）、一次 Meta mutation；最终 `published`；展示可信 Facebook 内容 ID / URL；公开资产 hash 与排队快照一致 | **本地 opt-in Playwright（非 CI 门禁）** + PostgreSQL + fake Meta request，配合 CAN-FB-03                                      |
| FB-PUB-02  | FB-PUB-01 已批准内容                                                                                                                                           | 双击；相同 key 重放；两个不同 key 并发；成功后再次点击                                                                                               | 每个内容 / Page 最多一次外部发布；重复返回稳定结果或冲突；不新增 PublishJob、Jobs、日志或 Meta mutation                                                                           | Playwright + integration 并发计数                                                                                             |
| FB-PUB-03  | fake Meta 在 provider I/O fence 后丢失响应                                                                                                                     | 点击发布并执行 Job；尝试普通 worker retry 和新点击                                                                                                   | Meta 只调用一次；PublishJob=`delivery_unknown`，记录时间 / 错误码 / 日志；queue 不产生 continuation 或自动重试；Portal 显示"结果未知"，不显示成功；新点击被阻止                   | Integration + Portal 读模型                                                                                                   |
| FB-PUB-04  | **CI 默认环境（`ADMIN_PORTAL_PUBLISHING_ENABLED=false`）**；另准备一条已排队任务                                                                               | 读取 UI；对真实 HTTP `publish-now` 端点发起命令；启动未注册 publication handler 的 Worker；尝试执行已有任务                                          | UI 不提供可用立即发布；真实 HTTP 命令返回 503 且零副作用；无新 PublishJob / Jobs、token lookup 或 Meta 调用；已有 pending Job 也不能越过开关外呼                                  | Playwright（跑在 CI 默认环境）+ integration 零副作用；命令层直调 fail closed 引用现有 `content-studio-publishing.test.ts:693` |
| FB-PUB-05A | 任务入队后账号变为 expired / disconnected、credential 缺失、publishing capability 未批准或账号身份不匹配                                                       | 执行原任务                                                                                                                                           | **现有绿色守卫回归**：在 provider I/O 前 fail closed；Meta 零调用；错误脱敏且可人工处理                                                                                           | Integration resolver / token provider                                                                                         |
| FB-PUB-05B | 任务入队后**只 bump `authorizationRevision`**；账号仍 connected，token 已配置且可读、未过期，publishing 仍 approved，平台账号身份不变                          | 执行原任务                                                                                                                                           | **当前 known-red**：修复前旧任务可能使用新授权继续发布；修复后在 provider I/O 前以 `stale_authorization_revision` fail closed，Meta 零调用                                        | Integration authority / token provider                                                                                        |
| FB-PUB-06  | fake Facebook Photos API 分别返回 `{id, post_id}`、仅 `{post_id}`、仅 `{id}`                                                                                   | 执行发布与状态 / permalink 归并                                                                                                                      | 已确认成功不得变成普通失败；可取得证据时写入最终 ID / URL；无法确认 permalink 时诚实进入待核验或 `delivery_unknown`，禁止盲重发                                                   | Transport + service adapter tests                                                                                             |

> 普通 CI 继续保持发布开关为 `false`，以维持“CI 不具备发布能力”的安全不变量。启用开关的测试 server 不会自行调用 Meta，外部调用只发生在测试进程装配的 fake Worker；因此是否增加第二个 `webServer` 是覆盖与成本取舍，不能以“Graph origin 硬编码”作为必然阻断。真实 Meta transport 仍只允许在受控 canary 使用。

> FB-PUB-05A 与 FB-PUB-05B 必须分开记录：expired / disconnected / 缺 credential / 未批准 capability 等是已有绿色守卫；revision-only 缺口已由 PR #94 修复并合并，任务快照会把 `authorizationRevision` 传给 resolver，在 provider I/O 前以 `stale_authorization_revision` fail closed。FB-PUB-01 已适配 PR #94 的 mutation + durable status obligation + continuation 状态机；本地实测确认三个 revision-scoped Jobs 均成功，不能再复用旧的“两 Jobs”断言。

### 4.7 飞书 CRM 字段与通知语义

| ID        | Setup                                                                             | Steps                                                         | Expected                                                                                                                                                                              | Evidence                                                                                               |
| --------- | --------------------------------------------------------------------------------- | ------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| FEISHU-01 | A 级 tender Lead；包含 `projectStage` 映射行的 active mapping；fake Feishu client | 执行 lead sync、new lead / high intent / handoff notification | `localLeadId`、公司、国家、意向、产品、项目阶段、面积、时间、图纸与本地 Lead 一致；`projectStage=tender` 来自 `lead.projectStage`，不能错误映射为 Lead 状态；每种通知 intent 最多一次 | fake Feishu payload snapshot；已随 PR #97 合并；`main@64e2627` 的 `feishu-sync` integration 13/13 通过 |
| FEISHU-02 | 无邮箱的 A 级社媒 Lead（依赖 blocker 1 修复）                                     | 通过真实 Feishu Job handler 执行 lead sync 与通知             | 无邮箱 Lead 也能同步，不抛异常、不伪造邮箱；平台身份作为联系来源写入远端记录；Job parser、mapper 和通知均接受空邮箱                                                                   | `tests/integration/feishu-sync.test.ts` + fake Feishu payload snapshot                                 |

> FEISHU-02 已在当前实施分支转绿：Job parser、revision、mapper、通知和默认 mapping 均接受空邮箱，完整 Facebook Page / sender / thread 身份追加到 Source / 联系方式，不伪造邮箱；飞书 contract 16/16、PostgreSQL integration 14/14 通过。
>
> 已在 contract 与 integration fixture 中补入 `projectStage` 映射，并分别锁定 mapper 与真实 Job handler 输出；contract 明确证明 `tender` 来自 `lead.projectStage`，integration 同时验证 A 级 Lead upsert 与三类通知。

## 5. 通用零副作用断言

所有签名、权限、幂等、状态、媒体、授权和 provider 失败分支统一验证：

- 没有额外 Conversation、Message、Lead、Handoff；
- 没有额外 Jobs、PublishJob、PublishLog、command receipt 成功结果；
- 没有额外 Meta / 飞书调用；
- token、签名密钥、访客 session token 不进入浏览器、URL、响应、日志或错误摘要；
- 一个批次中后续事件非法时，前面事件不会产生部分持久化；
- 重放返回稳定结果，不重新执行外部副作用；
- fake 通过只证明契约和本地链路，不改变平台 readiness。

## 6. 发布前真实 canary

| ID          | Setup                                                                 | Steps                                                              | Expected                                                                                                | Evidence                                   |
| ----------- | --------------------------------------------------------------------- | ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------- | ------------------------------------------ |
| CAN-CMS-01  | 生产环境；受控 admin；合成 EN / AR 内容                               | 在 `/dashboard` 发布产品、案例、文章代表内容；匿名读取；编辑并下架 | 公开页面、语言、RTL、缓存刷新和 404 与自动化一致；不进入 `/admin`；验收后清理合成内容                   | Portal / 官网截图、内容 ID、时间           |
| CAN-INQ-01  | 真实官网、Portal 与已启用飞书 mapping；合成询盘                       | 英文 / 阿语各提交一条；在 Portal 和飞书核对；重放一次              | 页面、Lead、飞书客户记录和提醒使用同一关联链；重复提交不新增客户或通知                                  | request / Lead / 飞书脱敏 ID、截图         |
| CAN-CHAT-01 | 正式已审核知识、active EN / AR Prompt、真实 AI provider、飞书 mapping | 英文 / 阿语各完成一个 2–3 轮代表场景；接管、回复、解决             | 回答有知识证据；高风险和不可用路径诚实转人工；Lead / 飞书 / Handoff 可追踪；不输出 token 或客户原始资料 | 会话 / Lead / 飞书关联证据、脱敏 usage log |
| CAN-FB-01   | 真实 Facebook 测试用户与 Page；正式 app / worker                      | 用户向 Page 发送普通咨询                                           | Meta event ID、Conversation、AI Message、delivery intent 与 Messenger 可见回复使用同一关联链            | 脱敏 ID、时间、Portal 截图、Messenger 截图 |
| CAN-FB-02   | CAN-FB-01；无邮箱高意向测试资料；Portal operator                      | 继续提供项目资格信息；执行 Lead / 飞书；接管并回复                 | 不伪造邮箱；平台身份成为 Lead 联系来源；飞书记录可追踪；接管后 AI 停止，人工回复在 Messenger 可见       | Lead / 飞书 / Handoff 关联证据             |
| CAN-FB-03   | 真实 Page；受控单张测试图片；发布窗口已批准                           | 工作台审核后点击一次发布；核对 Page；模拟或记录失败处理            | 取得官方内容 ID / URL；Page 内容一致；失败 canary 不自动重跑，结果未知时先人工核对                      | PublishJob / Log、官方 ID / URL、Page 截图 |

canary 直接在生产执行（见 §2.2），不纳入普通 CI，不自动删除已发布内容；是否删除由测试负责人在 Facebook 页面人工确认并记录。CAN-FB-03 同时承担 FB-PUB-01 发布 happy path 的对外证据责任。

canary 当前仅完成前置清单，**尚未获准执行**：自动化主线已合并且 `main@6066d5c` CI 成功，`xuemusi` 已完成共享结构 / 跨 owner Review；剩余前置是 jueyunai 明确批准 production 时间窗、执行人与停止条件，并人工核对服务端凭据、Page allowlist、官方权限、readiness 与 production worker smoke。执行时使用可识别、可清理的合成内容与单张受控图片；任何 `delivery_unknown`、重复内容、真实客户数据暴露、凭据异常或 worker / 主流程不通都立即停止，不自动重试，先在官方平台人工核对。

## 7. 当前已知阻断与诚实状态

| Blocker                             | 当前事实                                                                                                                                                                                                                                                                                                                                                                                                               | 对验收的影响                                                                                                                          |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| 1 社媒联系渠道（本地已关闭）       | 当前实施分支把 `Leads.email` 改为可空，并新增 server-managed、创建后不可变的 platform / account / sender / thread 身份；数据库约束要求 email、phone 或完整社媒身份至少一种，Portal 人工创建仍要求 email / phone；会话 repository、飞书和 Portal DTO / UI 已同步                                                                    | FB-IN-02B、FEISHU-02 已本地转绿；共享 Collection、migration 与跨人契约合并前仍需另一名开发者 Review                                |
| 2 飞书项目阶段（已关闭）            | `src/modules/feishu/mapLead.ts` 已将 `projectStage` 映射为 `lead.projectStage`，空值不写入；contract 与真实 PostgreSQL Job integration fixture 均包含该字段                                                                                                                                                                                                                                                            | FEISHU-01 已通过；后续回归必须保持 `tender` 不被 Lead `qualified` 状态覆盖                                                            |
| 3 Playwright Worker（已关闭）       | Facebook E2E support 已在测试进程装配一次性真实 JobWorker，并显式注入 deterministic responder、fake Meta 与 fake Feishu                                                                                                                                                                                                                                                                                                | FB-IN 首轮浏览器闭环已通过；真实 production worker wiring 仍必须由 canary 证明                                                        |
| 4 Webhook E2E 配置（已关闭）        | E2E server 已使用非敏感合成 `META_WEBHOOK_APP_SECRET`、verify token 和单一 Page allowlist；错误签名按固定服务端密钥验证                                                                                                                                                                                                                                                                                                | FB-IN 真实 HTTP route 可稳定执行，普通 E2E 仍不得继承真实 Meta 配置                                                                   |
| 5 发布任务授权 revision（已关闭）   | PR #94 已把 `PublishJob.authorizationRevision` 带入执行快照与 resolver 的 `expectedAuthorizationRevision`，并扩展 durable status / recovery 状态机                                                                                                                                                                                                                                                                     | FB-PUB-05B 已证明旧授权在 provider I/O 前阻断；FB-PUB-01 已按当前三 Job 状态机完成本地 Playwright 闭环                              |
| 6 Worker 装配漂移风险（非阻断加固） | Facebook E2E support 已显式注入 responder，避免首轮测试静默退化；生产 worker 仍硬接线真实 adapter                                                                                                                                                                                                                                                                                                                      | 不阻断当前 closure；真实 worker 进程 wiring 由 canary 证明，是否抽取通用注册工厂后置到 MVP 验收后                                     |
| 7 LinkedIn / Instagram              | 不进入 Facebook 首轮 canary，真实授权与 API 证据尚未纳入本文件完成条件                                                                                                                                                                                                                                                                                                                                                 | 保持 `conditional` / `blocked`；Portal readiness 使用 `action-required` / `blocked`，由 READY-02 断言，不得显示为已接入或已发布       |
| 8 发布开关覆盖缺口                  | CI 固定 `ADMIN_PORTAL_PUBLISHING_ENABLED=false`，同时拦截 UI 与真实 HTTP 命令层（见 §4.6）                                                                                                                                                                                                                                                                                                                             | 发布 happy path 不进 CI 门禁，由本地 FB-PUB-01 + CAN-FB-03 承担证据；§9 必须显式标注，禁止把绿色 CI 读成发布闭环已证                  |

## 8. 预期代码落点

测试目录：`tests/e2e/`、`tests/integration/`、`tests/contract/`

**既有覆盖必须先复用再扩展。** 以下原子能力已有对应用例，不得重复新建：

| 已覆盖能力                                                    | 现有位置                                                                                                                                                         |
| ------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| blocked 账号整批拒绝                                          | `tests/integration/platforms/meta-webhook-route.test.ts:156`                                                                                                     |
| 批次原子性 / legacy key / 领取后禁用 / lease 过期恢复         | `tests/integration/platforms/inbound-event.test.ts:172 / 207 / 383 / 794`                                                                                        |
| fence 阻断 handoff                                            | `tests/integration/platforms/conversation-delivery.test.ts:259`                                                                                                  |
| 重放 / kill switch / `delivery_unknown` / 并发 click key      | `tests/integration/platforms/content-studio-publishing.test.ts:436 / 693 / 719 / 781 / 888`                                                                      |
| lease + PublishJob revision CAS                               | `tests/integration/platforms/publishing-authority.test.ts:171 / 232`                                                                                             |
| authorization revision 自增                                   | `tests/integration/platforms/accounts.test.ts:165`                                                                                                               |
| Facebook `{id}` / `{post_id}` 解析与 permalink / unknown 归并 | `tests/unit/platforms/meta-publishing-requests.test.ts:429-483 / 546-590`、`tests/unit/platforms/publishing-service-adapter.test.ts:166-191 / 237-246 / 346-360` |

**仍缺的跨模块闭环**（本文件真正需要新增的窄范围 closure 测试）：

1. 无邮箱 Facebook Messenger 身份 → Lead → 飞书 → Portal；
2. AI / 知识不可用 → 可恢复人工路径且零伪造回答；
3. Content Studio 命令 → PublishJob → Jobs → Worker → fake Meta → Portal 最终状态。

| 步骤 | 用例                     | 操作                                                                                                                                                                                                              | 复用判断                                         | 代码位置                                                                                                                                                                                      |
| ---- | ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1    | 通用                     | Facebook fixture、一次性 Worker、fake Meta / Feishu、清理                                                                                                                                                         | 已完成，发布 harness 已扩展                      | `tests/e2e/admin-portal-facebook.support.ts`                                                                                                                                                  |
| 2    | FB-IN-01/02A/03          | Messenger HTTP 到 Portal 的浏览器闭环                                                                                                                                                                             | 已完成                                           | `tests/e2e/admin-portal-facebook-messenger.spec.ts`                                                                                                                                           |
| 3    | FB-IN-04/05/06           | 签名 / 权限 / fence / lease 断言                                                                                                                                                                                  | 已完成 / 复用既有                                | `tests/integration/platforms/meta-webhook-route.test.ts`、`inbound-event.test.ts`、`conversation-delivery.test.ts`                                                                            |
| 4    | FB-PUB-02/03/05A/05B/06  | 幂等 / unknown / 绿色授权守卫 / revision-only 阻断 / permalink 归并                                                                                                                                               | 已完成原子与共享状态机覆盖                       | `content-studio-publishing.test.ts`、`publishing-authority.test.ts`、resolver/token provider tests（FB-PUB-06 复用 `meta-publishing-requests.test.ts`、`publishing-service-adapter.test.ts`） |
| 5    | FB-PUB-01                | 本地发布点击闭环（缺口 3），非 CI 门禁                                                                                                                                                                            | 已完成；适配 PR #94 三 Job 状态机                | `tests/e2e/admin-portal-facebook-publishing.spec.ts` + `admin-portal-facebook.support.ts`                                                                                                     |
| 6    | FB-PUB-04                | CI 默认环境下 UI 无入口 + 真实 HTTP 503 零副作用                                                                                                                                                                  | 扩展                                             | `tests/e2e/admin-portal-content-studio.spec.ts`                                                                                                                                               |
| 7    | CMS-01                   | 三类内容发布 / 公开读取 / 下架 404 / RTL；SiteSettings EN / AR 与联系资料同步                                                                                                                                     | 已完成                                           | `tests/e2e/admin-portal-content.spec.ts` + `tests/e2e/website.spec.ts`                                                                                                                        |
| 8    | INQ-01                   | Portal 与飞书读回；CI 证据位于已排程 website suite                                                                                                                                                                | 已完成                                           | `tests/e2e/website.spec.ts`；`inquiry.spec.ts` 仅作 targeted helper / 补充验证                                                                                                                |
| 9    | FB-READY-01 / READY-02   | Facebook readiness、Worker smoke 与 IG / LinkedIn 诚实状态                                                                                                                                                        | 已完成自动化；真实 worker 待 canary              | `tests/e2e/admin-portal-platforms-operations.spec.ts` + Worker / readiness integration                                                                                                        |
| 10   | FEISHU-01 / 02           | `projectStage` 映射与无邮箱 Lead 同步                                                                                                                                                                             | 已完成并随 PR #101 合并；共享 Lead 结构已 Review | `tests/contract/feishu.test.ts` + `tests/integration/feishu-sync.test.ts`                                                                                                                     |
| 11   | WEB-CHAT-01/02           | 去掉内部 API mock 的真实客服闭环                                                                                                                                                                                  | 已完成；chat suite 5/5                           | `tests/e2e/website-chat-real.spec.ts` + `website-chat.support.ts`；`chat-handoff.spec.ts` 保留 UI 契约                                                                                        |
| 12   | 安全测试 Webhook 配置    | 补三个非敏感测试变量；**不修改发布开关**                                                                                                                                                                          | ⚠️ 修改                                          | `playwright.config.ts`                                                                                                                                                                        |
| 13   | 服务端 fake AI 测试出口  | 本地 `pnpm dev` 可使用 HTTP stub；CI `e2e:server` 为 production，当前没有 AI 专用 HTTP loopback 豁免，必须二选一：受信任 HTTPS stub，或新增环境门控的 `normalizeRuntimeBaseURL` 例外（后者改生产代码并需 review） | 测试设施 / 配置，或受控生产配置改动              | 测试侧 stub、Provider fixture、必要的 `playwright.config.ts` 环境；不得把现有 `IVYBM_E2E_ALLOW_HTTP_LOOPBACK` 误用于 AI Provider                                                              |
| 14   | Worker handler 注册工厂  | 抽出可注入 ports 的装配，测试与生产共用；作为后置加固，不是第一批前置                                                                                                                                             | ⚠️ 生产代码修改                                  | `src/worker.ts`                                                                                                                                                                               |
| 15   | 社媒身份 Lead 联系渠道   | 见 §7 blocker 1 连带面                                                                                                                                                                                            | ⚠️ 共享结构修改                                  | `Leads`、会话 repository、migration、生成类型、飞书类型与映射、Portal 表单 / DTO                                                                                                              |
| 16   | 飞书项目阶段映射         | 修正 + 补 fixture 映射行                                                                                                                                                                                          | ⚠️ 修改                                          | `src/modules/feishu/mapLead.ts` + `tests/contract/feishu.test.ts`                                                                                                                             |
| 17   | CI workflow / classifier | 不修改；新增 E2E 文件使用 `admin-portal-` 前缀以命中 admin 套件                                                                                                                                                   | 复用                                             | `scripts/ci/classify-changes.mjs:80`、`.github/workflows/ci.yml:343`                                                                                                                          |

第 8 项把需要 CI 证据的询盘闭环放入 `website.spec.ts`，因为 `classify-changes.mjs:76` 只排程该文件；原 `inquiry.spec.ts` 保留为 targeted / 辅助验证，不把它的绿色结果误读为正常 CI 已执行。WEB-CHAT 的真实闭环由 chat suite manifest 显式纳入 `website-chat-real.spec.ts`；原 `chat-handoff.spec.ts` 仍是分类入口与 UI 契约。新增 admin 侧 E2E 必须使用 `admin-portal-` 前缀才会命中 `:80`。

测试代码实施前，所有 `⚠️` 必须按 `ui-automation` 分析流程逐项确认；共享 Collection、migration、跨人契约、Webhook、生产代码 seam 和真实平台外部副作用仍需另一名开发者 Review。

## 9. 实施与验收门禁

开发 checkpoint 只运行与变更直接相关的 lint、typecheck、unit、contract、integration 或 E2E，不为测试数量重复运行全仓库门禁。转 Ready、合并和 production 发布继续接受现有 CI。

CI 排程事实，供判断证据是否真的被执行：

- `tests/integration/**` 变更触发 `database=true`（`scripts/ci/classify-changes.mjs:209`），集成证据会被排程；
- unit 与 contract 在 `code=true` 时始终运行（Fast CI），`src/modules/**` 变更由此覆盖；
- 修改 `playwright.config.ts` 触发 `enableAllE2e`（`classify-changes.mjs:282`），启用 website / admin / chat 三类套件，实际执行 `website.spec.ts`、`admin-visual.spec.ts`、`admin-portal-*.spec.ts`、`chat-handoff.spec.ts`；`inquiry.spec.ts`、`website-visual.spec.ts` 不在其中，只有 `full_fallback` 才运行全部 E2E 文件。因此 INQ-01 的 CI 门禁断言必须实现于 `website.spec.ts`，原 `inquiry.spec.ts` 只能作为 targeted 补充。

每条 P0 用例的验收记录至少包含：

- base / head SHA 和运行环境；
- 测试用例 ID、开始 / 结束时间、执行人；
- correlation ID 与脱敏的本地 / provider ID；
- 自动化命令与通过数量，**并确认 skipped=0 且实际执行用例数与预期一致**（12 个 `admin-portal-*` spec 中有 11 个在缺少 `E2E_ADMIN_EMAIL` / `E2E_ADMIN_PASSWORD` 时会静默 skip 且 exit code 仍为 0；CI 已在 job env 提供凭据，但本地与 canary 记录必须自证未被跳过）；
- 失败时的最终业务状态和零副作用读回；
- canary 的官方 ID / URL 或外部权限阻塞证据；
- 清理结果，以及未清理真实平台内容的人工说明；
- **FB-PUB-01 必须标注"非 CI 门禁，证据来自本地运行 + CAN-FB-03"**，禁止以 CI 通过替代。

只有自动化结果、真实 canary 和当前 head CI 一致通过，且已知 P0 blocker 清零，才能把对应闭环标记为完成。

## 10. 实施顺序

按生产代码、共享契约、Review 和回滚边界分批；同一边界内优先使用一个 Draft PR + 分阶段 commit，只有独立负责人、独立回滚或强制 Review 边界不同才拆 PR。

| 批次       | 范围                                                                                                    | 前置条件                                                                                                                                                        | 是否改生产代码                                                         |
| ---------- | ------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| 已合并批次 | FB-IN-01/02A/03/04/05/06、INQ-01、FB-READY-01、READY-02、CMS-01、FEISHU-01、WEB-CHAT-01、FB-PUB-05A/05B | 能力与回归均已进入 `main`；真实平台 canary 仍未执行                                                                                                             | 已包含必要的 Portal 缺陷修复、飞书 mapper 与共享发布状态机 Review      |
| 已合并     | FB-IN-02B、FEISHU-02、WEB-CHAT-02、FB-PUB-01                                                           | PR #101 已合并；定向门禁、共享 Review、current-head CI 与合并后 `main` CI 均成功                                                                                | 本地 / fake 证据不代表真实平台联调                                     |
| 本次补强   | FB-PUB-04                                                                                               | 在 CI 默认关闭发布开关时，补 UI 禁用、真实 HTTP 503 与 PublishJob / Jobs 零新增断言；本地 Admin E2E、typecheck、lint、unit 与定向 integration 已通过，待 current-head CI | 不修改发布开关、生产逻辑、CI workflow 或可信控制面                     |
| 待人工批准 | CAN-CMS / INQ / CHAT / FB                                                                               | 自动化主线已合并且 `main` CI 成功；由 jueyunai 另行批准受控 production 窗口                                                                                      | 会产生真实外部副作用，本批不执行                                       |

自动化主线已随 PR #101 合并并通过合并后 `main` CI；FB-PUB-04 浏览器负向证据补强已完成本地验证，待 current-head CI。该分支合并后，剩余工作仅为受控 canary；未经 jueyunai 再次明确批准不得访问 production 或真实 Meta / 飞书 / 付费 AI。发布 happy path 继续保持“本地 + canary”证据形态，普通 CI 的 publishing kill switch 不修改。
