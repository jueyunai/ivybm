# IVYBM 一期开发实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 在 23 个工作日内交付可上线的英文 / 阿语铝单板官网、统一 Payload CMS、官网 AI 客服与知识库、会话与线索闭环、飞书同步、AI 内容工作台，以及按前置条件分级验收的海外平台连接器。

**Architecture:** 使用一个 Next.js App Router + Payload CMS 模块化单体承载官网、后台、API 和轻量 worker，PostgreSQL 18.4 + pgvector 0.8.5 统一保存内容、会话、线索、任务和知识库。官网、CMS、AI 客服和飞书构成 P0 确定性闭环；Facebook Messenger、Instagram DM、TikTok 私信，以及 Facebook、Instagram、LinkedIn 图文发布属于 P1 条件性交付。WhatsApp 不在一期接入范围，二期再评估网页插件等替代方案。

**Tech Stack:** Node.js LTS、pnpm、TypeScript、Next.js App Router、Payload CMS、PostgreSQL 18.4、pgvector 0.8.5、Vitest、Playwright、Docker Compose、1Panel OpenResty、外部 AI API、飞书开放平台。

---

## 0. 执行规则

- 本计划从当前文档基线提交之后开始执行；每个任务独立验证，建议按任务提交。
- 初始化当天使用 Payload 官方脚手架支持的 Node / Next / React 兼容组合，随后由 `.nvmrc`、`package.json#packageManager` 和 `pnpm-lock.yaml` 固定。
- 测试驱动顺序：先写最小失败测试，再实现，再运行任务级测试，最后运行累计质量门禁。
- 第三方平台不阻塞 P0 主链路。P1 无真实授权时使用 fixture / mock / sandbox 完成契约测试，并记录阻塞条件。
- 真实 `.env`、客户数据、平台 token 和云密钥不进 Git；只提交 `.env.example`。
- 每阶段完成后更新 `docs/开发进度.md`。

## 1. 里程碑和依赖

| 里程碑          | 工作日 | 包含任务 | 可验收结果                                        | 负责人                                  |
| --------------- | -----: | -------- | ------------------------------------------------- | --------------------------------------- |
| M1 工程底座     |    1-3 | 1-3      | 应用、数据库、测试和 Docker 在本地可启动          | jueyunai                                |
| M2 CMS 可用     |    4-6 | 4-5      | 管理员可维护英文 / 阿语官网内容                   | jueyunai                                |
| M3 官网上线候选 |   7-10 | 6-7      | 客户确认原型高保真还原、SEO、询盘和线索入库可验收 | jueyunai                                |
| M4 AI 获客闭环  |  11-14 | 8-9      | 官网 AI 客服、知识引用、人工接管可用              | xuemusi（服务）+ jueyunai（官网 UI）    |
| M5 飞书与会话   |  15-17 | 10-11    | 线索同步飞书，失败可重试和补偿                    | jueyunai                                |
| M6 内容工作台   |  18-20 | 12       | 内容生成、审核、素材、发布页面和状态流可用        | jueyunai（工作台）+ xuemusi（发布接口） |
| M7 平台 PoC     |     21 | 13       | 平台能力矩阵、连接器或降级流程可验收              | xuemusi（平台服务）                     |
| M8 上线试运营   |  22-23 | 14-15    | 单一 production 上线、培训、冒烟和验收通过        | jueyunai（验收需两人共同确认）          |

分工对应 [`docs/requirements/一期需求说明文档.md`](../requirements/一期需求说明文档.md#L495) 第10节的板块划分；协作分支规范见 [`CONTRIBUTING.md`](../../CONTRIBUTING.md)。

**关键跨人依赖**：Task 9 由 xuemusi 提供会话 / AI / 人工接管服务，由 jueyunai 实现官网 ChatWidget；数据库集成读写 Task 7 创建的 `Leads`，但官网 UI 可以先依赖 `ChatService` mock 开发。Task 12 由 jueyunai 实现内容工作台、内部内容/审核流程以及 `PublishJobs` / `PublishLogs` 共享结构，由 xuemusi 提供第三方平台 capability / publish / status 服务；页面可以先依赖 `PublishingService` mock 开发。Task 13 会话侧数据库集成依赖 Task 9 的 `Conversations` / `Messages`，发布侧数据库集成依赖 Task 12 的 `PublishJobs` / `PublishLogs`，真实 Webhook 异步处理、发布执行、失败重试和人工补偿依赖 Task 10 的 `Jobs` 与 worker。数据库阶段必须等待对应 Collection、migration、Payload 注册和生成类型全部合并。

**接口解耦与 mock 先行**：Task 9 / Task 12 开始实现前，双方先共同 review TypeScript port/interface、请求响应 schema、错误码、状态枚举和 fixture。jueyunai 使用 fake service 完成 UI、交互和 E2E；xuemusi 使用 fake repository、fake provider 和官方结构 fixture 完成服务、契约测试和平台 adapter。真实实现替换 mock 时不得要求消费者导入平台 SDK、模型 SDK 或读取 token；破坏性 contract 变更必须同时更新 mock、contract test 和双方消费者，并由另一名开发者 review。

**并行空间**：Task 8（知识库与模型网关）只依赖 Task 1-3，不依赖 Task 4-7，xuemusi可以在 jueyunai 做 Task 4-7 期间提前并行开始，不必等到第 11 天，两人日历总工期有机会比顺序执行的 23 天更短；如果要重排并行版排期，需要另外设计。

---

### Task 1: 初始化正式 Payload / Next 工程与质量门禁

**Files:**

- Create: `.nvmrc`
- Create: `package.json`
- Create: `pnpm-lock.yaml`
- Create: `tsconfig.json`
- Create: `next.config.mjs`
- Create: `src/payload.config.ts`
- Create: `src/app/(payload)/admin/[[...segments]]/page.tsx`
- Create: `src/app/(frontend)/layout.tsx`
- Create: `src/app/(frontend)/page.tsx`
- Create: `vitest.config.ts`
- Create: `playwright.config.ts`
- Create: `tests/unit/smoke.test.ts`
- Create: `.env.example`
- Modify: `.gitignore`
- Modify: `README.md`

**Step 1: 固定运行时和包管理器**

运行 Payload 官方初始化流程，选择 PostgreSQL、TypeScript 和空白项目模板；把生成内容直接放在仓库根目录，不创建额外 monorepo。

Expected: `package.json` 包含与 Payload 兼容的 Next / React 版本，`packageManager` 固定 pnpm；`.nvmrc` 与 Docker 后续使用相同 Node 主版本。

**Step 2: 写失败的工程冒烟测试**

在 `tests/unit/smoke.test.ts` 断言项目标识、默认 locale 和健康检查模块导出。此时健康检查模块尚不存在。

Run: `pnpm vitest run tests/unit/smoke.test.ts`

Expected: FAIL，提示健康检查模块不存在。

**Step 3: 添加最小健康模块和脚本**

创建 `src/lib/health.ts`，返回应用名、状态和版本；在 `package.json` 增加 `lint`、`typecheck`、`test:unit`、`test:e2e`、`build` 脚本。

**Step 4: 验证工程底座**

Run:

```bash
pnpm install --frozen-lockfile
pnpm lint
pnpm typecheck
pnpm test:unit
pnpm build
```

Expected: 所有命令退出码为 0；构建产生 Next.js production 输出。

**Step 5: Commit**

```bash
git add .
git commit -m "feat: initialize Payload and Next.js application"
```

---

### Task 2: PostgreSQL、pgvector、迁移和种子数据

**Files:**

- Create: `compose.yaml`
- Create: `compose.override.yaml`
- Create: `docker/postgres/init/001-extensions.sql`
- Create: `src/migrations/`
- Create: `src/seed/index.ts`
- Create: `scripts/db/reset-test.ts`
- Create: `tests/integration/database.test.ts`
- Create: `vitest.integration.config.mts`
- Modify: `.env.example`
- Modify: `.github/workflows/ci.yml`
- Modify: `package.json`
- Modify: `src/payload.config.ts`

**Step 1: 写数据库失败测试**

测试数据库连接、`vector` 扩展可用，以及迁移后能读写最小记录。

Run: `pnpm test:integration -- tests/integration/database.test.ts`

Expected: FAIL，数据库服务或迁移尚不存在。

**Step 2: 实现本地数据库服务**

`compose.yaml` 使用 PostgreSQL 18.4 + pgvector 0.8.5，配置持久化卷、健康检查和日志轮转；`compose.override.yaml` 仅暴露本地开发端口。

**Step 3: 建立迁移和种子命令**

关闭 Payload 开发环境隐式 schema push，固定 `src/migrations` 为迁移目录；增加 `db:migrate`、`db:migrate:create`、`db:seed`、`db:reset:test`。当前数据模型只允许种入幂等演示管理员，基础站点设置和演示内容在 Task 4 对应模型建立后扩展，任何阶段都不包含真实客户数据。

**Step 3.1: 将数据库集成测试接入 CI**

CI 使用临时 PostgreSQL 18.4 + pgvector 0.8.5 service，执行 migration 和数据库集成测试；不得依赖本地持久化卷或真实密钥。

**Step 4: 验证数据库生命周期**

Run:

```bash
docker compose up -d db
pnpm db:migrate
pnpm db:seed
pnpm test:integration -- tests/integration/database.test.ts
docker compose down
```

Expected: PostgreSQL healthy，vector 扩展存在，测试通过，数据卷未被 `down` 删除。

**Step 5: Commit**

```bash
git add compose.yaml compose.override.yaml docker src/migrations src/seed scripts/db tests/integration vitest.integration.config.mts .env.example .github/workflows/ci.yml package.json pnpm-lock.yaml src/payload.config.ts docs/plans/2026-07-16-一期开发实施计划.md
git commit -m "feat: add PostgreSQL development environment"
```

---

### Task 3: 用户认证、角色权限、审计和健康检查

**Files:**

- Create: `src/collections/Users.ts`
- Create: `src/collections/AuditLogs.ts`
- Create: `src/access/roles.ts`
- Create: `src/hooks/writeAuditLog.ts`
- Create: `src/app/api/health/live/route.ts`
- Create: `src/app/api/health/ready/route.ts`
- Create: `tests/unit/access/roles.test.ts`
- Create: `tests/integration/health.test.ts`
- Modify: `src/payload.config.ts`

**Step 1: 写权限矩阵失败测试**

覆盖 admin、operator、sales 对用户管理、内容、平台账号、会话和线索的读写权限。

Run: `pnpm test:unit -- tests/unit/access/roles.test.ts`

Expected: FAIL，权限函数尚不存在。

**Step 2: 实现最小角色模型**

实现三角色访问控制；后台禁止公开注册；关键操作调用审计 hook。

**Step 3: 写并实现健康检查**

`live` 只检查应用进程，`ready` 检查 Payload / PostgreSQL；不得在响应中暴露密钥和连接串。

Run: `pnpm test:integration -- tests/integration/health.test.ts`

Expected: 数据库可用时 200；数据库不可用时 `ready` 返回 503，`live` 仍返回 200。

**Step 4: 累计验证**

Run: `pnpm lint && pnpm typecheck && pnpm test:unit && pnpm test:integration`

Expected: PASS。

**Step 5: Commit**

```bash
git add src tests
git commit -m "feat: add authentication roles and health checks"
```

---

### Task 4: CMS 核心 Collections 和多语言字段

**Files:**

- Create: `src/collections/Media.ts`
- Create: `src/collections/Pages.ts`
- Create: `src/collections/ProductCategories.ts`
- Create: `src/collections/Products.ts`
- Create: `src/collections/Projects.ts`
- Create: `src/collections/Posts.ts`
- Create: `src/collections/Downloads.ts`
- Create: `src/globals/SiteSettings.ts`
- Create: `src/fields/seo.ts`
- Create: `src/fields/slug.ts`
- Create: `tests/integration/cms-collections.test.ts`
- Modify: `src/payload.config.ts`

**Step 1: 写 CMS 失败测试**

测试英文 / 阿语标题、唯一 slug、草稿发布、媒体 alt、SEO 字段及引用完整性。

Run: `pnpm test:integration -- tests/integration/cms-collections.test.ts`

Expected: FAIL，Collections 尚不存在。

**Step 2: 实现共享字段和 Collections**

所有公开内容支持 `en` / `ar`；中文仅作为后台备注时使用非公开字段。页面、产品、项目、文章支持草稿和版本。

**Step 3: 生成并运行迁移**

Run:

```bash
pnpm db:migrate:create cms-core
pnpm db:migrate
pnpm test:integration -- tests/integration/cms-collections.test.ts
```

Expected: PASS；数据库结构由 migration 创建。

**Step 4: 后台冒烟验证**

Run: `pnpm dev`

Expected: 管理员可登录 `/admin`，新增并发布英文 / 阿语产品、案例和文章。

**Step 5: Commit**

```bash
git add src tests
git commit -m "feat: add multilingual CMS content models"
```

---

### Task 5: CMS 权限、媒体处理和演示内容

**Files:**

- Create: `src/access/content.ts`
- Create: `src/hooks/revalidateContent.ts`
- Create: `src/seed/content.ts`
- Create: `tests/unit/access/content.test.ts`
- Create: `tests/unit/hooks/revalidate-content.test.ts`
- Create: `tests/integration/content-access.test.ts`
- Create: `tests/integration/media.test.ts`
- Create: `src/migrations/<timestamp>_cms_media_policy.ts`
- Create: `src/migrations/<timestamp>_cms_media_policy.json`
- Modify: `src/collections/Media.ts`
- Modify: `src/collections/Pages.ts`
- Modify: `src/collections/ProductCategories.ts`
- Modify: `src/collections/Products.ts`
- Modify: `src/collections/Projects.ts`
- Modify: `src/collections/Posts.ts`
- Modify: `src/collections/Downloads.ts`
- Modify: `src/globals/SiteSettings.ts`
- Modify: `src/seed/index.ts`
- Modify: `src/payload.config.ts`
- Modify: `src/migrations/index.ts`

**Step 1: 写内容权限和媒体失败测试**

验证未发布内容不对公网开放、operator 可编辑内容、sales 不可删除内容；上传拒绝超限或不允许类型。

**Step 2: 实现权限和媒体边界**

图片生成适配尺寸，记录 alt 和版权来源；PDF / 图片限制 MIME 和文件大小；发布后触发相关页面 revalidate。

**Step 3: 导入不含版权风险的演示内容**

只使用项目自有占位图或明确标注的临时素材；不得把 Unsplash 图片当正式资产。

**Step 4: 验证**

Run: `pnpm test:unit -- tests/unit/access/content.test.ts && pnpm test:integration -- tests/integration/media.test.ts`

Expected: PASS。

**Step 5: Commit**

```bash
git add src tests
git commit -m "feat: enforce CMS content and media policies"
```

---

### Task 6: 英文 / 阿语官网、RTL 和 SEO

**Files:**

- Create: `src/app/(frontend)/[locale]/layout.tsx`
- Create: `src/app/(frontend)/[locale]/page.tsx`
- Create: `src/app/(frontend)/[locale]/about/page.tsx`
- Create: `src/app/(frontend)/[locale]/products/page.tsx`
- Create: `src/app/(frontend)/[locale]/products/[slug]/page.tsx`
- Create: `src/app/(frontend)/[locale]/projects/page.tsx`
- Create: `src/app/(frontend)/[locale]/projects/[slug]/page.tsx`
- Create: `src/app/(frontend)/[locale]/news/page.tsx`
- Create: `src/app/(frontend)/[locale]/news/[slug]/page.tsx`
- Create: `src/app/(frontend)/[locale]/contact/page.tsx`
- Create: `src/app/sitemap.ts`
- Create: `src/app/robots.ts`
- Create: `src/lib/i18n.ts`
- Create: `src/lib/seo.ts`
- Create: `tests/e2e/website.spec.ts`
- Create: `tests/e2e/website-visual.spec.ts`
- Create: `tests/unit/seo.test.ts`

**Step 1: 写 SEO 和 E2E 失败测试**

覆盖 `/en`、`/ar`、RTL、canonical、hreflang、metadata、JSON-LD、404、sitemap 和 robots；同时以 `references/website-prototype/` 为客户确认基准，覆盖首页、About、Products、Projects、News、Contact 的英文 / 阿语和桌面 / 平板 / 移动截图，以及导航、Hero 轮播、产品筛选、规格表、移动菜单、语言切换和表单状态。

**Step 2: 高保真还原客户确认原型**

`references/website-prototype/` 是客户提供并确认的正式官网 UI / 交互验收基准，不是普通参考。使用 Next.js / Payload 重写组件时，必须高保真还原其视觉层级、版式、色彩、字体、间距、组件状态、导航 / 轮播 / 筛选 / 表单交互、响应式，以及英文 LTR / 阿拉伯语 RTL 效果。只替换占位联系方式、外部演示图片、假数据、模拟提交成功和纯客户端路由 / 数据逻辑；任何主动改版必须先取得客户确认。

**Step 3: 实现服务端内容读取和缓存失效**

公开页只读取已发布内容；产品、项目、文章支持稳定 locale URL 和动态 metadata。

**Step 4: 验证**

Run:

```bash
pnpm test:unit -- tests/unit/seo.test.ts
pnpm test:e2e -- tests/e2e/website.spec.ts tests/e2e/website-visual.spec.ts
pnpm build
```

Expected: PASS；阿语页面 `dir=rtl`；构建无缺失 metadata 错误；关键页面和交互状态在英文 / 阿语及桌面 / 平板 / 移动视口通过客户原型截图基准，正式页面无外部图片热链、占位联系方式或 demo / fake 内容。

**Step 5: Commit**

```bash
git add src tests
git commit -m "feat: build multilingual SEO website"
```

---

### Task 7: 询盘表单、线索模型和反滥用

**Files:**

- Create: `src/collections/Leads.ts`
- Create: `src/collections/LeadSources.ts`
- Create: `src/app/api/inquiries/route.ts`
- Create: `src/components/inquiry/InquiryForm.tsx`
- Create: `src/lib/validation/inquiry.ts`
- Create: `src/lib/security/rateLimit.ts`
- Create: `tests/unit/inquiry-validation.test.ts`
- Create: `tests/integration/inquiries.test.ts`
- Create: `tests/e2e/inquiry.spec.ts`

**Step 1: 写验证、限流和幂等失败测试**

覆盖必填、邮箱、电话、语言、来源、honeypot、重复提交和限流。

**Step 2: 实现线索入库**

接口返回 requestId；保存来源 URL / UTM，但只收集业务所需字段；失败不向前端暴露数据库详情。

**Step 3: 实现表单体验**

英文 / 阿语错误信息、提交中状态、成功状态和可恢复失败；无 JavaScript 时仍有可理解的基础行为。

**Step 4: 验证**

Run: `pnpm test:unit && pnpm test:integration -- tests/integration/inquiries.test.ts && pnpm test:e2e -- tests/e2e/inquiry.spec.ts`

Expected: PASS；重复提交不产生重复线索。

**Step 5: Commit**

```bash
git add src tests
git commit -m "feat: add secure inquiry and lead capture"
```

---

### Task 8: 知识库、文档切片、向量检索和模型网关

**Files:**

- Create: `src/collections/KnowledgeDocuments.ts`
- Create: `src/collections/KnowledgeChunks.ts`
- Create: `src/collections/PromptTemplates.ts`
- Create: `src/modules/ai/gateway.ts`
- Create: `src/modules/ai/providers/openaiCompatible.ts`
- Create: `src/modules/knowledge/chunk.ts`
- Create: `src/modules/knowledge/retrieve.ts`
- Create: `src/modules/knowledge/embed.ts`
- Create: `tests/unit/knowledge/chunk.test.ts`
- Create: `tests/integration/knowledge/retrieve.test.ts`
- Create: `tests/contract/ai-gateway.test.ts`
- Create: `tests/fixtures/ai/`

**Step 1: 写切片、检索和网关失败测试**

测试稳定切片、locale、来源引用、向量检索排序、超时和供应商错误归一化。

**Step 2: 实现知识库导入和检索**

保存来源、版本、语言和审核状态；只有已审核知识可用于正式回复。

**Step 3: 实现供应商无关网关**

统一 `generateText`、`embed`、超时、错误、模型名称、token 和成本记录；测试使用 fake provider，不调用真实付费 API。

**Step 4: 验证**

Run: `pnpm test:unit -- tests/unit/knowledge && pnpm test:integration -- tests/integration/knowledge && pnpm test:contract -- tests/contract/ai-gateway.test.ts`

Expected: PASS；测试期间没有外部网络依赖。

**Step 5: Commit**

```bash
git add src tests
git commit -m "feat: add knowledge retrieval and AI gateway"
```

---

### Task 9: 官网 AI 客服、意向评分和人工接管

**责任拆分：** jueyunai 负责官网 ChatWidget、运营会话/接管界面、英文 / 阿语 / RTL 交互和前端 E2E；xuemusi 负责会话、AI 回复、知识引用、意向评分，以及人工接管状态机、转换守卫、幂等、权限、审计、领域事件和数据库集成。双方共同维护 contract、状态枚举、错误码与 fixture；具体决策见 [`ADR-0001`](../architecture/adr/0001-human-handoff-domain-boundary.md)。

**Files:**

- Create: `src/modules/conversations/contracts.ts`（共享 contract）
- Create: `tests/fakes/chatService.ts`（jueyunai 前端 mock）
- Create: `src/collections/VisitorSessions.ts`
- Create: `src/collections/Conversations.ts`
- Create: `src/collections/Messages.ts`
- Create: `src/collections/Handoffs.ts`
- Create: `src/modules/conversations/service.ts`
- Create: `src/modules/leads/score.ts`
- Create: `src/app/api/chat/sessions/route.ts`
- Create: `src/app/api/chat/messages/route.ts`
- Create: `src/components/chat/ChatWidget.tsx`（jueyunai）
- Create: `tests/unit/lead-score.test.ts`
- Create: `tests/integration/chat.test.ts`
- Create: `tests/e2e/chat-handoff.spec.ts`（jueyunai）

**Step 1: 冻结 ChatService contract 和 mock**

双方共同确认开始会话、发送消息、重试、请求接管、运营认领、解决会话的请求响应 schema、错误码和状态枚举；提交 fake service 和 contract fixture。前端只能表达接管意图，不能直接写最终状态或负责人；此阶段不依赖数据库、模型供应商或真实页面。

**Step 2: 并行实现官网 ChatWidget**

jueyunai 使用 `ChatService` mock 完成英文 / 阿语、RTL、加载、失败重试、知识引用和人工接管 UI，不直接访问模型 SDK 或会话 Collection。

**Step 3: 写对话状态机和评分失败测试**

覆盖语言识别、2-3 轮信息收集、敏感主题转人工、高意向漏判样本、重复消息、重复/并发接管、非法状态转换、权限、审计、通知失败可见性，以及进入 `human_active` 后 AI 停止回复。

**Step 4: 实现统一会话服务**

官网只是第一个 channel；服务层使用统一 conversation / message / handoff 模型，为后续平台连接器复用。Next Route 只做鉴权、校验和 HTTP 映射，权威状态由 `ConversationService` 维护；服务产生 `handoff.created` 事件，由 Task 10 / 11 处理通知和补偿。

**Step 5: 实现知识引用和安全回复**

回复保存引用知识、prompt 版本和模型记录；无可靠知识时明确转人工，不编造价格和交期。

**Step 6: 联调与验证**

Run: `pnpm test:unit -- tests/unit/lead-score.test.ts && pnpm test:integration -- tests/integration/chat.test.ts && pnpm test:e2e -- tests/e2e/chat-handoff.spec.ts`

Expected: PASS；高风险问题触发接管；高意向线索被创建。

**Step 7: 分支和 PR**

contract、后端服务和官网 UI 可以使用同一 Task 下的独立短分支并行提交；contract PR 必须双方 review，后端与 UI PR 均需证明 mock 和真实实现通过同一契约测试。

---

### Task 10: PostgreSQL 任务队列、重试和人工补偿

**Files:**

- Create: `src/collections/Jobs.ts`
- Create: `src/modules/jobs/claim.ts`
- Create: `src/modules/jobs/worker.ts`
- Create: `src/modules/jobs/retry.ts`
- Create: `src/worker.ts`
- Create: `tests/unit/jobs/retry.test.ts`
- Create: `tests/integration/jobs/worker.test.ts`
- Modify: `compose.yaml`
- Modify: `package.json`

**Step 1: 写状态机、租约和重试失败测试**

覆盖并发抢占、租约过期、指数退避、最多 5 次、dead job 和人工重试。

**Step 2: 实现数据库任务队列**

不引入 Redis；app 与 worker 可使用同一镜像、不同启动命令。任务处理器必须幂等。

**Step 3: 加入 worker 容器和健康策略**

worker 不暴露公网端口；异常退出由 Compose 重启，但 dead job 不无限循环。

**Step 4: 验证**

Run: `pnpm test:unit -- tests/unit/jobs && pnpm test:integration -- tests/integration/jobs && docker compose config`

Expected: PASS；Compose 配置有效。

**Step 5: Commit**

```bash
git add src tests compose.yaml package.json pnpm-lock.yaml
git commit -m "feat: add durable PostgreSQL job worker"
```

---

### Task 11: 飞书 CRM 同步、提醒和失败补偿

**Files:**

- Create: `src/collections/FeishuMappings.ts`
- Create: `src/modules/feishu/client.ts`
- Create: `src/modules/feishu/mapLead.ts`
- Create: `src/modules/feishu/syncLead.ts`
- Create: `src/modules/feishu/notify.ts`
- Create: `tests/contract/feishu.test.ts`
- Create: `tests/integration/feishu-sync.test.ts`
- Create: `tests/fixtures/feishu/`
- Modify: `.env.example`

**Step 1: 写飞书契约和字段映射失败测试**

覆盖新增、更新、重复同步、字段缺失、限流、token 失效和失败重试。

**Step 2: 实现适配器和映射**

业务层不依赖飞书字段 ID；映射配置独立保存。同步使用本地 lead ID 作为幂等依据。

**Step 3: 实现提醒规则**

新高意向、跟进到期和同步失败通知；微信 / 企业微信兜底不在未确认前实现，只保留通知接口。

**Step 4: 验证**

Run: `pnpm test:contract -- tests/contract/feishu.test.ts && pnpm test:integration -- tests/integration/feishu-sync.test.ts`

Expected: PASS；mock 限流会进入重试；重复任务不创建重复客户。

**Step 5: Commit**

```bash
git add src tests .env.example
git commit -m "feat: sync leads and reminders to Feishu"
```

---

### Task 12: AI 内容工作台、素材、审核和发布任务

**责任拆分：** jueyunai 负责内容工作台页面、内容生成与审核流程、状态机以及 `PublishJobs` / `PublishLogs` 共享结构；xuemusi 通过 Task 13 提供第三方平台 capability / publish / status 服务、平台 adapter 和发布结果回调。双方共同维护发布 contract、错误码和 fixture。

**Files:**

- Create: `src/modules/publishing/contracts.ts`（共享 contract）
- Create: `tests/fakes/publishingService.ts`（jueyunai 前端 mock）
- Create: `src/components/content-workbench/ContentWorkbench.tsx`（jueyunai）
- Create: `tests/e2e/content-workbench.spec.ts`（jueyunai）
- Create: `src/collections/Assets.ts`
- Create: `src/collections/GeneratedContents.ts`
- Create: `src/collections/ContentReviews.ts`
- Create: `src/collections/PublishJobs.ts`
- Create: `src/collections/PublishLogs.ts`
- Create: `src/modules/content/generate.ts`
- Create: `src/modules/content/format.ts`
- Create: `src/modules/publishing/stateMachine.ts`
- Create: `tests/unit/content-format.test.ts`
- Create: `tests/unit/publish-state.test.ts`
- Create: `tests/integration/content-workbench.test.ts`

**Step 1: 冻结 PublishingService contract 和 mock**

双方共同确认 Facebook 图文、Instagram 图文、LinkedIn 图文的平台能力查询、提交发布任务、读取状态和失败重试的请求响应 schema、错误码、状态枚举与 fixture。页面不得接收平台 token，也不得导入平台 SDK；TikTok 和 WhatsApp 不在一期发布 contract 中。

**Step 2: 并行实现内容工作台页面**

jueyunai 使用 `PublishingService` mock 完成内容生成、审核、Facebook / Instagram / LinkedIn 平台选择、定时发布、发布中、成功、失败和重试 UI；mock 覆盖 conditional / blocked 平台能力。

**Step 3: 写内容与发布状态机失败测试**

覆盖 draft → review → approved → scheduled → published / failed；未审核内容不得发布。

**Step 4: 实现多语言内容生成和平台格式化**

生成英文 / 阿语文案、图片提示词和视频脚本；事实字段引用已审核知识；平台格式化不直接调用第三方。

**Step 5: 实现审核和任务创建**

operator 可审核，sales 只读；审核、退回和发布动作写审计日志。

**Step 6: 联调与验证**

Run: `pnpm test:unit -- tests/unit/content-format.test.ts tests/unit/publish-state.test.ts && pnpm test:integration -- tests/integration/content-workbench.test.ts`

Expected: PASS；未审核内容无法进入发布任务。

**Step 7: 分支和 PR**

内容工作台和第三方平台服务使用独立短分支并行提交；共享 `PublishJobs` / `PublishLogs`、PublishingService contract 和跨人接口必须双方 review。

---

### Task 13: 平台连接器 PoC 和降级交付

**责任边界：** xuemusi 负责第三方平台服务和 adapter，并向 Task 12 提供冻结的 `PublishingService` contract；jueyunai 的内容工作台只消费该内部接口和 mock，不直接接入平台 SDK、token 或 Webhook。社媒会话出站遵循 [ADR-0003](../architecture/adr/0003-social-conversation-outbound-delivery.md)：在没有账号 / 授权时保持诚实的人工接管，先冻结 server-only port / fake / fixture；真实自动发送必须另走持久 Job 与平台 adapter，不能把未投递文案标为已发送。

**Files:**

- Create: `src/modules/platforms/types.ts`
- Create / Modify: `src/modules/platforms/ports.ts`（接口 / 纯逻辑阶段声明 `PlatformConversationOutboundPort`、请求 / 结果与错误契约）
- Create: `src/modules/platforms/webhook.ts`
- Create: `src/modules/platforms/meta/connector.ts`
- Create: `src/modules/platforms/linkedin/export.ts`
- Create: `src/modules/platforms/tiktok/connector.ts`
- Create: `src/modules/platforms/fakeConversationOutboundPort.ts`（接口 / 纯逻辑阶段）
- Modify: `src/modules/conversations/contracts.ts`
- Modify: `src/collections/Conversations.ts`
- Modify: `src/collections/VisitorSessions.ts`
- Create: `src/migrations/<timestamp>_task13_tiktok_channel.ts`
- Create: `src/migrations/<timestamp>_task13_tiktok_channel.json`
- Modify: `src/migrations/index.ts`
- Modify: `src/payload-types.ts`
- Create: `tests/fakes/platformEventRepository.ts`
- Create: `src/app/api/webhooks/meta/route.ts` (database integration stage only)
- Create: `src/collections/PlatformAccounts.ts` (database integration stage only)
- Create: `tests/contract/platforms/meta.test.ts`
- Create: `tests/contract/platforms/tiktok.test.ts`
- Create: `tests/contract/platforms/outbound.test.ts`
- Create: `tests/unit/platforms/webhook.test.ts`
- Create: `tests/unit/platforms/fake-conversation-outbound-port.test.ts`
- Create: `tests/integration/platforms/` (database integration stage only)
- Create: `tests/fixtures/platforms/`
- Create: `docs/research/一期平台接入PoC记录.md`
- Modify: `.env.example`

**Step 1: 写接口 / 纯逻辑失败测试**

使用官方结构 fixture、mock 和 fake repository，覆盖签名错误、时间戳过期、重复 `platform + externalEventId`、入站消息归一化、发布结果回调，以及社媒会话出站的重复 `deliveryKey`、限流 / retryable 错误、人工接管抑制和 fake 网络隔离；测试不访问真实平台网络。

**Step 2: 实现 TypeScript ports 和统一连接器接口**

连接器只负责外部 payload 与内部归一化事件的转换；通过 port/interface 表达会话写入、发布任务查询、幂等事件仓库，以及 server-only 的会话出站投递请求。出站 port 的 fake 只返回明确的模拟结果，不能把 fake 结果回写为真实消息状态。依赖未合并前不创建临时 `Conversations`、`Messages`、`PublishJobs` 或 `PublishLogs`，不生成替代 migration；token 不写入日志。

**Step 3: 依赖合并后实现数据库 adapters**

- 所有数据库 adapter 都必须等待对应 Collection、migration、`src/payload.config.ts` 注册和 `src/payload-types.ts` 生成类型全部合并到 `main`；随后从最新 `origin/main` 更新分支。
- Task 9 的 `Conversations` / `Messages` 完整合并后，实现入站会话 / 消息 adapter 和集成测试。
- 社媒 AI 自动出站必须由 `ConversationService` 判定仍处于 `ai_active`，在入队和 worker 执行前二次校验；不得从 webhook HTTP handler 同步调用平台。实现持久 delivery Job / outbox、重试、dead job 与人工补偿前，Task 10 Jobs / worker 和 `PlatformAccounts` 与其 migration / Payload 注册 / 类型必须已合并。
- 在实现 TikTok adapter 前，以新的前向 migration 将会话 channel 演进为支持 `tiktok`；不得编辑已合并的 Task 9 migration。WhatsApp 不得作为一期 connector 的可选入站 channel；如数据库中存在历史值，迁移和展示必须保留其历史可读性，但不允许新的一期自动接入路径写入该值。
- Task 12 的 `PublishJobs` / `PublishLogs` 完整合并后，再实现发布任务 / 结果回调 adapter 和集成测试。
- Task 10 的 `Jobs` Collection、worker、migration、Payload 注册和生成类型完整合并后，才能实现真实 Webhook 事件先落库再异步消费、发布执行、失败重试、dead job 和人工补偿。纯连接器接口和 fixture 契约测试不依赖 Task 10。
- `PlatformAccounts` 及其 migration 在这一阶段基于已合并的最新 migration 历史生成，不在纯逻辑阶段提前生成。

**Step 4: 按外部条件执行 PoC**

本步骤中的自动投递实测仍受 ADR-0003 的数据库 / worker 门槛约束；账号、官方 schema、`PlatformAccounts` 与持久 Job 未就绪时，不执行自动回复的真实发送测试。

- 有 Meta 测试资产：以 Facebook Messenger、Instagram DM 的企业 / 商业账号、主页、App Review、Webhook 订阅和允许的消息发送权限为条件，在 production 的受控发布窗口验证入站、AI 自动回复的真实投递，以及 Facebook / Instagram 图文测试发布。
- 有 TikTok 测试资产：以商业账号、目标地区的官方私信 API、应用授权及审核为条件，验证 TikTok 私信 Webhook / 入站消息与会话自动回复；无权限或无官方 schema 时保留 fixture、配置说明和阻塞证据。
- LinkedIn：实现图文平台格式化、文案复制和素材打包下载；账号类型不限制，只有获得 API 发布权限时再启用自动发布。
- 无权限或审核未完成：使用官方结构 fixture 完成契约测试，记录缺失资产、申请状态和阻塞证据。WhatsApp 不执行一期连接器或实测，二期再评估网页插件等替代接入。

**Step 5: 分阶段验证**

Run（接口 / 纯逻辑阶段）：`pnpm test:unit -- tests/unit/platforms && pnpm test:contract -- tests/contract/platforms`

Run（数据库集成阶段）：`pnpm test:integration -- tests/integration/platforms`

Expected: 已开始阶段的测试全部 PASS；PoC 文档明确一期平台为 `available / conditional / blocked`，并区分代码契约完成、数据库依赖阻塞和外部授权阻塞。出站 fake / fixture 通过只表示接口契约完成，绝不代表 AI 文案已经投递；只有在 production 或等价真实受控环境完成账号授权、Webhook 和入站 / 出站目标操作实测后才能标记 `available`。WhatsApp 作为二期候选不进入一期状态矩阵。

**Step 6: Commit**

```bash
git add src tests docs/research/一期平台接入PoC记录.md .env.example
git commit -m "feat: add conditional platform connector PoCs"
```

---

### Task 14: 单一 production 镜像、Compose、监控和 CI

**Files:**

- Modify: `Dockerfile`
- Modify: `.dockerignore`
- Create: `compose.prod.yaml`
- Create: `docker/postgres/postgresql.conf`
- Create: `scripts/smoke-test.sh`
- Modify: `.github/workflows/ci.yml`
- Create: `.github/workflows/build-image.yml`
- Create: `docs/operations/部署手册.md`
- Create: `tests/operations/compose.test.ts`

**Step 1: 写生产配置失败检查**

检查容器非 root、健康检查、只暴露必要端口、日志轮转、内存限制、持久化卷、必需变量，以及 production Compose 只能引用 SHA tag + image digest 的不可变镜像、不得使用 `build:`。

**Step 2: 实现 standalone 多阶段镜像**

保留 standalone 多阶段镜像；CI 在质量门禁通过后将 runtime 和 worker 按 Git commit SHA tag 推送到私有镜像仓库（默认 GHCR，实施前确认），并记录 image digest 供生产固定引用。生产只带运行依赖，服务器不现场构建；`PAYLOAD_SECRET`、数据库密码和 AI Key 只能在运行时注入，不能作为 Docker build argument。2C4G 首轮验收以 app 约 768MB、worker 约 384MB、db 约 768MB 为初始上限并压测，必须为系统、1Panel 和 OpenResty 留出余量。

**Step 3: 实现 1Panel 手动发布路径**

新增仅引用私有镜像仓库 Git SHA tag + image digest 的 `compose.prod.yaml`，通过 1Panel Compose 手动 `pull / redeploy`。迁移使用一次性 migrate 服务，成功后启动 app / worker；服务器上的受限环境文件保存运行时 secret、`IMAGE_TAG`、runtime / worker digest 和生产 URL。当前开发阶段不配置自动备份；因此禁止不可逆 migration，也不允许把真实不可替代客户数据作为无备份环境的测试数据。

**Step 4: 验证**

Run:

```bash
pnpm lint
pnpm typecheck
pnpm test:unit
pnpm test:integration
pnpm test:contract
pnpm build
docker build -t ivybm:test .
docker compose -f compose.yaml -f compose.prod.yaml config
pnpm test:operations
```

在 1Panel 手动发布后运行：`./scripts/smoke-test.sh https://ivybm.com`。Expected: 全部通过；runtime / worker 镜像能启动，迁移成功，`/api/health/live` 与 `/api/health/ready` 返回 200；发布过程不产生服务器本地构建或 secret 泄漏。

**Step 5: Commit**

```bash
git add Dockerfile .dockerignore compose.prod.yaml docker scripts .github docs/operations tests/operations
git commit -m "feat: add manual production deployment tooling"
```

---

### Task 15: 单一 production 上线、验收和培训

**Files:**

- Create: `docs/operations/上线检查表.md`
- Create: `docs/operations/回滚方案.md`
- Create: `docs/acceptance/一期验收清单.md`
- Create: `docs/acceptance/试运营问题记录.md`
- Create: `docs/training/后台使用说明.md`
- Create: `tests/e2e/acceptance.spec.ts`
- Modify: `docs/开发进度.md`

**Step 1: 配置单一 production 入口并手动发布**

在阿里云 DNS 添加 `@` 的 A 记录、`www` 的 CNAME 记录以及 `panel` 的 A 记录，均指向已确认的生产入口；配置 `ivybm.com` / `www.ivybm.com` 的 OpenResty HTTPS 与 canonical。将 1Panel 面板绑定为 `panel.ivybm.com`，保持面板在其独立端口（当前为 8090），启用面板 TLS、安全入口、两步验证和授权 IP `199.83.211.101`；域名绑定不替代 IP 白名单。腾讯云防火墙仅公开 80/443，8090 仅允许该固定 IP，移除公网 22；服务器控制由腾讯云 Lighthouse MCP 完成。随后在 1Panel 手动选择经 CI 推送的 SHA tag + digest 镜像，执行 `pull / redeploy`、migration 和 smoke test。

Expected: `/api/health/ready` 返回 200；官网和后台可通过 HTTPS 访问；1Panel 仅从 `https://panel.ivybm.com:<面板端口>/<安全入口>` 且固定 IP 访问。

**Step 2: 执行一期验收测试**

Run:

```bash
BASE_URL=https://ivybm.com pnpm test:e2e -- tests/e2e/acceptance.spec.ts tests/e2e/website-visual.spec.ts
./scripts/smoke-test.sh https://ivybm.com
```

Expected: 官网高保真还原客户确认原型，英文 / 阿语、桌面 / 平板 / 移动视觉回归与 RTL 通过；CMS 发布、询盘、AI 客服、人工接管、飞书同步、内容审核和平台降级流程全部通过对应等级验收。

**Step 3: 执行镜像回滚演练**

在 1Panel 保留当前和上一版本完整的 SHA tag + digest 对。部署上一版本 runtime / worker 镜像并重新执行健康检查，验证应用镜像回滚路径；数据库 migration 必须保持向前兼容，当前无备份阶段不得执行需要数据回退的破坏性变更。

Expected: 上一版本镜像可被手动 pull / redeploy，官网与 `/api/health/ready` 恢复正常；回滚说明明确不包含数据库回退。

**Step 4: 培训与试运营观察**

完成 admin / operator / sales 培训。上线后进行 48 小时重点观察，监测健康检查、容器重启、磁盘和日志轮转；自动备份是后续承载不可替代业务数据前必须另行立项的运维项。

**Step 5: 最终质量门禁**

Run:

```bash
pnpm lint
pnpm typecheck
pnpm test:unit
pnpm test:integration
pnpm test:contract
pnpm test:e2e
pnpm build
```

Expected: 全部通过；验收清单无 P0 阻塞项，P1 平台项有明确状态和证据，二期项单独标注。

**Step 6: Commit**

```bash
git add docs tests/e2e/acceptance.spec.ts
git commit -m "docs: complete phase one launch and acceptance"
```

---

## 2. 全局完成定义

一期只有同时满足以下条件才视为完成：

1. 英文 / 阿语官网可通过正式 HTTPS 域名访问，核心页面、交互、响应式和 RTL 高保真还原客户确认的 `references/website-prototype/`，视觉回归、SEO 和 CMS 更新正常。
2. 官网询盘和 AI 客服会话可以沉淀线索，高风险问题可转人工，高意向线索能进入飞书。
3. 内容工作台具备生成、人工审核、平台格式化、发布任务和日志，不允许未审核内容直接发布。
4. P0 自有能力全部实测通过；Facebook Messenger、Instagram DM、TikTok 私信及 Facebook、Instagram、LinkedIn 图文发布按账号、地区与审核条件完成实接、PoC 或阻塞证据；WhatsApp 与未列平台为二期，不作为一期验收项。
5. 权限、审计、Webhook 验签、幂等、重试、dead job 和人工补偿均有自动化测试。
6. production 镜像、Compose、健康检查、日志轮转、1Panel 手动拉镜像发布和应用镜像回滚经过验证；当前开发阶段的备份延期及其数据风险已明确记录。
7. 无真实密钥、合同、客户原始资料、数据库和上传文件进入 Git。
8. `docs/acceptance/一期验收清单.md` 无未处理 P0 阻塞项，试运营关键问题已记录并关闭或明确延期。
