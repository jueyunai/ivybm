# Feishu contract fixtures

These fixtures contain only synthetic identifiers and official response-shaped data. They do not
contain a tenant ID, app credential, Bitable token, customer record, or production recipient ID.
