/** Backwards-compatible command path for clients that call the ingestion action directly. */
export { POST } from '../sources/route'

export const dynamic = 'force-dynamic'
export const runtime = 'nodejs'
